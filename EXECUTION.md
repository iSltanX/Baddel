# بدّل — خطة العمل التنفيذية

**طريقة الاستخدام:** افتح جلسة Claude Code في مجلد المشروع على أي جهاز Mac، واكتب **«ابدأ المرحلة X»**.
الجلسة تبدأ بلا أي سياق سابق. تقرأ [CLAUDE.md](CLAUDE.md) للبروتوكول وسياسة الوكلاء، و[STATUS.md](STATUS.md) للحالة، ثم قسم المرحلة هنا.

الوثائق المرجعية:
- **ماذا نبني ولماذا:** [docs/baddel-implementation-plan.md](docs/baddel-implementation-plan.md)
- **نصوص الواجهة وقواعد التصميم:** [docs/baddel-figma-prompt-pro.md](docs/baddel-figma-prompt-pro.md). كُتبت كبرومبت Figma، لكنها **المواصفة المعتمدة للواجهة**: البنية والنصوص والحالات والألوان والخطوط وقواعد RTL. الواجهة تُبنى مباشرة HTML/Svelte بلا مرحلة Figma.

**التقنية:** Tauri 2 (نواة Rust + واجهة Svelte/TypeScript/Vite، وnpm) على نمط Luma. قرار التحويل من Swift وأسبابه في الخطة التفصيلية §2.

## نظرة عامة

| # | المرحلة | الناتج المرئي | يحتاج منك | التقدير |
|---|---|---|---|---|
| 0 | تجهيز البيئة والهيكل | تطبيق فارغ يُبنى ويظهر في شريط القوائم بلا أيقونة Dock | قرار مستودع GitHub | جلسة قصيرة |
| 1 | النواة | `cargo test` أخضر: «اثممخ» ⇄ `hello` | — | جلسة |
| 2 | طبقة النظام | الاختصار يعمل فعليًا في TextEdit وSafari وVS Code | منح صلاحية Accessibility | جلسة–جلستان |
| 3 | الواجهة | القائمة + الإعدادات + الترحيب + HUD | مراجعة الشكل في المتصفح | جلستان |
| 4 | الصقل والاختبار اليدوي | مصفوفة التطبيقات خضراء | تجربة يدوية معي | جلسة |
| 5 | الأيقونة والتوقيع والتحديث | DMG موقَّع يتحدّث تلقائيًا | قرار التوقيع + اعتماد الأيقونة | جلسة |
| 6 | الإطلاق | مستودع عام + إصدار v1.0.0 | موافقة على النشر | جلسة قصيرة |

**فحص البيئة (أول كل مرحلة):**
```bash
source scripts/env.sh
sw_vers -productVersion; xcode-select -p; rustc --version; cargo --version; node --version; npm --version; git status --short | head
```
- المتطلبات: macOS 13+، وXcode Command Line Tools، وRust stable (1.80+)، وNode 20+.
- `scripts/env.sh` يعالج أجهزة nvm: يزيل دوال الصدفة الكسولة (`node` و`npm`…) التي تنهار في الصدفة غير التفاعلية، ويضيف أحدث نسخة Node إلى PATH. **ابدأ به كل أمر Bash يستخدم node أو npm**، والسكربتات في `scripts/` تستدعيه بنفسها.
- إن نقص شيء فأخبر المستخدم بأمر التثبيت وتوقف. لا تثبّت أدوات نظام دون إذن.

---

## المرحلة 0 — تجهيز البيئة والهيكل

**الهدف:** مستودع نظيف وتطبيق Tauri يُبنى ويعرض أيقونة مؤقتة في شريط القوائم، بلا أيقونة Dock وبلا أي نافذة عند الإقلاع.

**الخطوات**
1. فحص البيئة.
2. `git init`، ثم `.gitignore` (يتجاهل `node_modules/` و`dist/` و`target/` و`.DS_Store` و`*.dmg`)، ورخصة MIT باسم Sultan Al-Anzi.
3. الهيكل:
   ```
   package.json · vite.config.ts · svelte.config.js · tsconfig.json · index.html
   src/                         # واجهة Svelte 5 + TS (فارغة تقريبًا الآن)
   Cargo.toml                   # workspace: crates/baddel-core + src-tauri
   crates/baddel-core/          # منطق صِرف بلا اعتماد على Tauri ولا macOS (اختبار واحد شكلي)
   src-tauri/
     ├── tauri.conf.json        # بلا نوافذ افتراضية · macOSPrivateApi: false · identifier: com.isltanx.baddel
     ├── capabilities/default.json
     └── src/main.rs · lib.rs   # tray بقائمة فيها «إنهاء» فقط · ActivationPolicy::Accessory
   scripts/check.sh             # cargo test --workspace && npm run check && npm run build
   ```
4. إضافات Tauri تُضاف الآن وتُستخدم لاحقًا: `global-shortcut` و`autostart` و`updater` و`store` و`process`.
5. اسأل المستخدم: هل يُنشأ مستودع GitHub الآن؟ (خاص حتى الإطلاق، باسم `Baddel`.) نفّذ الإنشاء بعد موافقته فقط.

**الوكلاء:** لا حاجة.

**معيار القبول**
```bash
./scripts/check.sh            # الاختبارات تمر والواجهة تُبنى
npm run tauri build -- --debug   # ينتج Baddel.app
```
إضافة إلى تشغيل التطبيق المبني والتأكد من ثلاثة أمور: الأيقونة في شريط القوائم، ولا أيقونة في Dock، و«إنهاء» يعمل.

---

## المرحلة 1 — النواة (Core)

**الهدف:** منطق التحويل كاملًا داخل `crates/baddel-core`، Rust صِرف، ومغطّى بالاختبارات.
**المرجع:** الخطة التفصيلية §4.1–4.3 و§6.

**الخطوات**
1. `layout.rs`: النوع `LayoutMap` بخريطتي `ar→en` و`en→ar`، وtrait باسم `LayoutProvider` يعطي `(keycode, modifiers) → String` لكل تخطيط. المخرجات متعددة المحارف («لا») تُخزَّن كسلاسل. عند التصادم تُقدَّم الطبقة الأدنى ({بلا، ⇧، ⌥، ⌥⇧}).
   - `StaticLayouts`: خريطة Arabic‑PC ⇄ ABC مضمَّنة، تُستخدم احتياطًا وأساسًا للاختبار، فتعمل الاختبارات على أي جهاز وعلى CI.
   - المزوّد الحي (TIS + `UCKeyTranslate`) **ليس هنا**، بل في `src-tauri` بالمرحلة 2، لتبقى النواة بلا اعتماد على النظام.
2. `convert.rs`: كشف الاتجاه بأغلبية المحارف، ومطابقة الأطول أولًا، وتمرير ما ليس في الخريطة كما هو. يعيد `Conversion { text, direction }`.
3. `disambiguate.rs`: لكل «لا» في اتجاه ar→en يولّد الاحتمالات (`b` أو `gh`)، ويختار الموجود في قائمة الكلمات، والافتراضي `b`. سقف 2⁶ احتمالًا، وبعده الافتراضي مباشرة.
4. `assets/en-words.txt`: نحو 10 آلاف كلمة شائعة (`include_str!`)، حروف صغيرة، كلمة في كل سطر، من مصدر برخصة حرة يوثَّق في `THIRD_PARTY.md`.
5. الاختبارات (40 فأكثر): ذهاب وإياب `convert(convert(x)) == x` لكل مفاتيح الخريطة · «اثممخ»→`hello` · `sghl`→«سلام» · `light` و`about` و`bright` · حروف كبيرة ⇄ طبقة ⇧ · أرقام ومسافات ورموز · نص مختلط · سلسلة فارغة · تشكيل.

**الوكلاء (اختياري — 2 بالتوازي، sonnet)**
- **أ:** تجهيز `en-words.txt`: جلب قائمة كلمات شائعة برخصة حرة، تنظيفها (a–z فقط، بلا تكرار، مرتّبة)، وكتابة المصدر والرخصة. يلمس `crates/baddel-core/assets/en-words.txt` و`THIRD_PARTY.md` فقط.
- **ب:** جدول حالات اختبار: 60 زوجًا `(input, expected, note)` بصيغة `const CASES: &[(&str, &str, &str)]` حسب تخطيط Arabic‑PC المعطى له نصًا في البرومبت. يلمس `crates/baddel-core/tests/fixtures.rs` فقط.
- الجلسة الرئيسية تراجع عينة من كل مخرَج قبل اعتماده. **أخطاء الجدول تُصحَّح في الجدول لا في الكود.**

**معيار القبول**
```bash
cargo test -p baddel-core     # 40+ اختبارًا تمر
cargo clippy -p baddel-core -- -D warnings
```

---

## المرحلة 2 — طبقة النظام

**الهدف:** الاختصار يحوّل النص فعليًا في التطبيقات الحقيقية. الواجهة ما زالت قائمة tray بدائية.
**المرجع:** الخطة التفصيلية §4.4 و§5 و§6.
**الاعتماديات المسموحة هنا:** `core-foundation` و`core-graphics` و`objc2` و`objc2-foundation` و`objc2-app-kit` و`accessibility-sys`. دوال Carbon (TIS\* و`UCKeyTranslate` و`IsSecureEventInputEnabled`) تُعرَّف في كتلة `extern "C"` يدوية داخل `sys/carbon.rs`.

**الخطوات** (كلها في `src-tauri/src/sys/`. كل `unsafe` محصور هنا خلف واجهات آمنة، ومعه تعليق يبرر السلامة)
1. `permissions.rs`: `AXIsProcessTrustedWithOptions` + فحص دوري يبثّ حدثًا للواجهة + فتح لوحة Privacy › Accessibility.
2. `keysynth.rs`: توليد ⌥⇧← و⌘C و⌘V عبر `CGEvent`، مع مهلات مسمّاة في مكان واحد.
3. `pasteboard.rs`: لقطة كاملة لكل `pasteboardItems` بجميع أنواعها واستعادتها. الكتابة المؤقتة موسومة بـ`org.nspasteboard.TransientType` و`ConcealedType`. انتظر تغيّر `changeCount` بمهلة قصوى 150ms بدل `sleep` ثابت.
4. `text_access.rs`: AX أولًا (`kAXSelectedTextAttribute` من العنصر المركَّز)، ثم السقوط إلى مسار الحافظة.
5. `input_source.rs`: المزوّد الحي لـ`LayoutProvider` عبر TIS + `UCKeyTranslate`، و`TISSelectInputSource` بعد التحويل، وإعادة بناء الخريطة عند `kTISNotifyEnabledKeyboardInputSourcesChanged`.
6. `controller.rs`: مسار §4.4 كاملًا، ويشمل Secure Input واستثناء التطبيقات بالـbundle id وحدّ 10 آلاف محرف وآخر كلمة والمدّ عند التكرار خلال ثانيتين والتراجع (من الذاكرة فقط). يعمل على خيط خاص لا على الخيط الرئيسي، ونداءات AppKit تُرحَّل إلى الخيط الرئيسي.
7. ربط الاختصار عبر `tauri-plugin-global-shortcut` (الافتراضي ⌥⇧Space). التنفيذ عند **رفع** المفاتيح (`Released`) حتى لا تختلط معدِّلات الاختصار بالضغطات المولَّدة.
8. **اطلب من المستخدم** منح صلاحية Accessibility للبناء الحالي، ثم جرّبا معًا.

**الوكلاء:** لا. هذه الطبقة حسّاسة ومترابطة وتُنفَّذ في الجلسة الرئيسية كلها.

**معيار القبول** (يدوي مع المستخدم، وتُسجَّل النتيجة في STATUS.md)
- TextEdit (مسار AX): تحويل تحديد ✓ · آخر كلمة ✓ · المدّ كلمتين ✓
- Safari في حقل ويب، وVS Code (مسار الحافظة): تحويل تحديد ✓ · آخر كلمة ✓
- الحافظة بعد كل عملية تطابق ما قبلها، وتُختبر بنص ثم بصورة ✓
- في حقل كلمة مرور لا يحدث شيء ✓
- لغة لوحة المفاتيح تتبدّل بعد التحويل ✓
- `./scripts/check.sh` يمرّ، و`cargo clippy --workspace -- -D warnings` نظيف.

---

## المرحلة 3 — الواجهة

**الهدف:** كل الشاشات بالعربية RTL والإنجليزية، فاتح وداكن، حسب المواصفة.
**المرجع:** [docs/baddel-figma-prompt-pro.md](docs/baddel-figma-prompt-pro.md) — البرومبتات 0–5 (البنية والنصوص والحالات والألوان وسلّم الخطوط وقائمة التدقيق في البرومبت 7).
**المرجع البصري:** [docs/design-reference.md](docs/design-reference.md) — أولًا **تصميم Stitch الذي اعتمده المستخدم** (لقطاته في `docs/design/stitch/`)، ثم ملفا Figma Make لتفاصيل المكوّنات. لكلٍّ قائمة «ما يُعتمد» و«ما يُصحَّح». عند التعارض في البنية أو النصوص تُقدَّم المواصفة.

**مبادئ معمارية**
- **القائمة أصيلة** (Tauri `Menu` على الـtray)، لا webview. القوائم الأصيلة لا تقبل عناصر مخصصة، فيُعرض رأس الحالة وسطر التلميح **عنصرين نصيين معطَّلين**. نصوصها من جدول ترجمة صغير في Rust.
- **HUD أصيل:** `NSPanel` عبر `objc2` بخصائص nonactivating وignoresMouseEvents ومستوى عائم. لا يُبنى بـwebview، لأن نافذة الويب قد تسرق التركيز من التطبيق الهدف وتُبقي عملية webview حيّة في الخمول. خط Almarai يُسجَّل عبر `CTFontManager`.
- **الإعدادات والترحيب** نافذتا webview **تُنشآن عند الطلب وتُدمَّران عند الإغلاق**، فلا عملية webview في الخمول.
- كل حالة التطبيق في Rust (`tauri-plugin-store`)، والواجهة تقرأ وتكتب عبر أوامر `invoke` وتستمع للأحداث. لا منطق في الواجهة.

**الخطوات**
1. `src/lib/theme.css`: متغيرات الألوان بالوضعين (`prefers-color-scheme`)، وسلّم الخطوط، والمسافات، وحلقة التركيز. خطّا Cairo وAlmarai مضمَّنان محليًا (`@font-face`) **بلا أي طلب شبكي**.
2. `src/lib/i18n/{ar,en}.json` + مخزن لغة بسيط، و`dir` يتبع اللغة. النصوص العربية تُنقل حرفيًا من المواصفة.
3. المكوّنات (Svelte): Keycap · ShortcutRecorder (يلتقط `keydown` ← صيغة accelerator ← يجرّب التسجيل في Rust، وفشله يعني حالة conflict) · FormRow · GroupCard · PermissionCard · ConversionSample · AppListRow · Button · PaneToolbar.
4. **الإعدادات** (560pt عرضًا، وشريط أقسام علوي): الأقسام الخمسة. معاينة خريطة المفاتيح تُرسم من `LayoutMap` الحية عبر أمر Rust، والاستثناءات عبر `tauri-plugin-dialog` على `/Applications` (تُضاف الإضافة هنا)، وتشغيل عند الدخول عبر `autostart`.
5. **الترحيب** (560×600 بثلاث خطوات): أول تشغيل فقط، وزر «تخطَّ»، وبطاقة الصلاحية بحالاتها الحية، وحقل التجربة يعمل فعليًا مع `controller`. يمكن إعادة فتحها من قسم «حول».
6. **القائمة:** الحالات الثلاث (جاهز / تحتاج صلاحية / متوقف)، وآخر تحويل مع «تراجع»، وعنصر ✓، و⌘, و⌘Q. أيقونة template بحالاتها الثلاث (مؤقتة حتى المرحلة 5).
7. **HUD:** الحالات الخمس، والحركة حسب المواصفة مع احترام Reduce Motion.
8. مراجعة RTL وإمكانية الوصول حسب قائمة البرومبت 7: تنقّل كامل بلوحة المفاتيح، وأدوار ARIA للمبدّلات والأقسام، وتباين 4.5:1 فأكثر.

> أثناء التطوير تُراجع الشاشات في المتصفح عبر `npm run dev` مع بيانات وهمية لأوامر Rust، فهذه هي «مرحلة التصميم» بدل Figma.

**الوكلاء (اختياري — 2 بالتوازي، sonnet)**
- **أ:** إنشاء `ar.json` و`en.json`: استخراج كل النصوص العربية من المواصفة وكتابة مقابلها الإنجليزي بمفاتيح منظمة (`menu.*` و`settings.*` و`onboarding.*` و`hud.*`). يلمس هذين الملفين فقط.
- **ب:** تنزيل ملفات الخطين (Cairo 600/700 وAlmarai 400/700، بصيغتي woff2 وttf) ورخصتَي OFL إلى `src/assets/fonts/`، وتحديث `THIRD_PARTY.md`. لا يلمس غير ذلك.

**معيار القبول**
- `./scripts/check.sh` يمرّ (ويشمل `svelte-check`).
- لقطات لكل شاشة × (عربي/إنجليزي) × (فاتح/داكن) في `docs/screenshots/` ويراجعها المستخدم.
- تغيير أي إعداد ينعكس فورًا، ويبقى محفوظًا بعد إعادة التشغيل.
- بعد إغلاق الإعدادات لا توجد عملية WebKit تابعة للتطبيق (`pgrep -lf Baddel`).
- ظهور HUD لا يسحب التركيز من TextEdit.

---

## المرحلة 4 — الصقل والاختبار اليدوي

**الهدف:** سلوك موثوق في التطبيقات الشائعة، ومعالجة ما تكشفه التجربة.
**المرجع:** الخطة التفصيلية §6 و§8.

**الخطوات**
1. نفّذ مع المستخدم مصفوفة §8 كاملة، وسجّل النتائج جدولًا في `docs/test-matrix.md`.
2. أصلح الإخفاقات. لكل إصلاح في منطق التحويل يُكتب اختبار في `baddel-core`، وفي طبقة النظام تُسجَّل ملاحظة في المصفوفة.
3. اضبط المهلات على أبطأ تطبيق ناجح (ثوابت مسمّاة في مكان واحد).
4. الاستثناءات الافتراضية: Terminal وiTerm ومديرو كلمات المرور الشائعون (بالـbundle id).
5. تحقّق من الخصوصية: لا يُكتب نص المستخدم في `println!` أو `log` أو `console.log` أو على القرص، وCSP مقيَّدة في `tauri.conf.json`، ولا اتصال شبكي غير المحدِّث.
6. قياس: زمن الاختصار إلى الاستبدال (الهدف أقل من 150ms في مسار AX وأقل من 350ms في مسار الحافظة)، والذاكرة في الخمول (الهدف أقل من 40MB بلا نوافذ مفتوحة).

**الوكلاء (اختياري — 2 بالتوازي، sonnet)**
- **أ:** مراجعة خصوصية بالقراءة فقط: أي تسجيل أو كتابة لنص المستخدم أو استدعاء شبكي، في Rust وTS. يعيد قائمة `ملف:سطر`. لا يعدّل شيئًا.
- **ب:** مراجعة اتساق الترجمة بالقراءة فقط: مفاتيح ناقصة أو غير مستخدمة بين `ar.json` و`en.json` والكود، ونصوص مكتوبة مباشرة في المكوّنات أو في Rust. لا يعدّل شيئًا.

**معيار القبول**
- `docs/test-matrix.md` مكتمل، وكل صف فيه ✓ أو مستثنى بسبب موثَّق.
- `./scripts/check.sh` يمرّ، ومراجعتا الوكيلين بلا ملاحظات مفتوحة.

---

## المرحلة 5 — الأيقونة والتوقيع والتحديث

**الهدف:** حزمة قابلة للتوزيع تتحدّث تلقائيًا دون أن تفقد صلاحية Accessibility.
**يتوقف على المستخدم:** قرار التوقيع + اعتماد تصميم الأيقونة.

**الخطوات**
1. **الأيقونة:** تُرسم طبقات SVG يدويًا في `design/icon/` (`0-background` … `3-keycap-front`) حسب البرومبت 6 في المواصفة، وتُعرض على المستخدم لاعتمادها. ثم تُدمج في 1024px ← `npm run tauri icon` ← `.icns`. أيقونة tray بصيغة template (PNG أسود مع alpha، @1x/@2x) بحالاتها الثلاث.
   - _اختياري لاحقًا:_ أيقونة Liquid Glass عبر Icon Composer. هذه تتطلب تجميع `Assets.car` بـ`actool` وإضافته للحزمة، وتُسجَّل فكرةً لما بعد v1.
2. **التوقيع:**
   - Developer ID: متغيرات `APPLE_SIGNING_IDENTITY` و`APPLE_ID` و`APPLE_PASSWORD` و`APPLE_TEAM_ID` يضبطها المستخدم في بيئته بنفسه، **ولا تُكتب في أي ملف**. Tauri يتولى التوقيع وNotarization أثناء `tauri build`.
   - شهادة ذاتية ثابتة: توثيق إنشائها مرة واحدة في `docs/signing.md` واستخدامها في كل إصدار عبر `signingIdentity`.
   - في الحالتين: تحقق أن `codesign -dr - Baddel.app` يعطي **designated requirement ثابتًا** بين بناءين، فهو ما يحفظ صلاحية Accessibility عبر التحديثات.
3. **المحدِّث:** `tauri signer generate` (المفتاح الخاص خارج المستودع، والعام في `tauri.conf.json`)، وendpoint يشير إلى `latest.json` في GitHub Releases، وربط «تحقّق من التحديثات» والمبدّل التلقائي.
4. `scripts/release.sh <version>`: رفع النسخة في الملفات الثلاثة ← `tauri build --target universal-apple-darwin` ← DMG + `.app.tar.gz` + `.sig` ← توليد `latest.json`.
5. اختبار التحديث: ثبّت 0.9.0، وامنح الصلاحية، ثم حدّث إلى 0.9.1 عبر المحدِّث، ويجب أن تبقى الصلاحية ممنوحة.

**الوكلاء:** لا. التوقيع والمفاتيح لا تُفوَّض.

**معيار القبول**
- `./scripts/release.sh 0.9.1` ينتج DMG يُثبَّت ويعمل على حساب مستخدم نظيف.
- اختبار التحديث في الخطوة 5 ناجح والصلاحية باقية.
- `spctl -a -vv Baddel.app` يعطي `accepted` (في حالة Developer ID).
- لا أسرار في المستودع: `git grep -iE "BEGIN (EC|PRIVATE)|password|secret key"` فارغ.

---

## المرحلة 6 — الإطلاق

**الهدف:** v1.0.0 منشور بصفحة مستودع على مستوى رفّ وجُسور.

**الخطوات**
1. `README.md` عربي على نمط مستودعات المستخدم (غلاف، شارات، رابط تنزيل، «ما هو بدّل؟»، GIF، الخصوصية، التثبيت، الأسئلة المتكررة، وتنبيه Gatekeeper إن لم يكن Developer ID) + `README.en.md`.
2. `CHANGELOG.md` و`PRIVACY.md` (قصير: لا شبكة، لا سجل، صلاحية واحدة وسببها).
3. GIF توضيحي مدته 6–8 ثوانٍ: كتابة «اثممخ» ← اختصار ← `hello` + HUD. يسجّله المستخدم، ونجهّز له السيناريو والمقاسات.
4. غلاف المستودع وصورة المعاينة الاجتماعية تُبنيان HTML/SVG بهوية التطبيق، وتُصدَّران PNG.
5. **بعد موافقة صريحة من المستخدم:** جعل المستودع عامًا، ووسم `v1.0.0`، وGitHub Release مع DMG و`latest.json`.
6. مسودة منشور إطلاق قصير بالعربية، ينشره المستخدم بنفسه.

**الوكلاء (اختياري — 2 بالتوازي، sonnet)**
- **أ:** مسودة `README.en.md` ترجمةً عن النسخة العربية المعتمدة، بالبنية نفسها. يلمس هذا الملف فقط.
- **ب:** مسودة `CHANGELOG.md` من `git log`، و`PRIVACY.md` من §5 في الخطة التفصيلية. يلمس هذين الملفين فقط.

**معيار القبول**
- تنزيل DMG من صفحة الإصدار على جهاز آخر ← تثبيت ← ترحيب ← تحويل ناجح.
- روابط README كلها تعمل، والصور تظهر في الوضعين على GitHub.
- STATUS.md: كل المراحل ✅.

---

## ما بعد v1.0

أفكار v1.1 فما بعد في الخطة التفصيلية §10، ومنها نشر `baddel-core` حزمة Rust مستقلة (نواة `arabic-text-kit`) ودمجها في نفّذ. تُفتح كمراحل جديدة (7، 8، …) بالصيغة نفسها.
