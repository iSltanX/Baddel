# سجل التغييرات

جميع التغييرات المهمة في «بدّل» موثّقة هنا.

## v1.0.0 — 2026-09-23

الإصدار العلني الأول: تصحيح النص المكتوب بتخطيط لوحة المفاتيح الخطأ، عربي ⇄
إنجليزي، باختصار واحد يعمل في أي تطبيق تقريبًا عبر تسهيلات الاستخدام وحدها.

### التحويل

- **تحويل التحديد أو آخر كلمة**: حدّد نصًا ثم اضغط الاختصار فيُستبدل في مكانه؛
  بلا تحديد يُحوَّل ما قبل المؤشر.
- **مدّ التحويل**: ضغطة ثانية خلال ثانيتين تمدّ التحويل كلمةً أخرى للخلف،
  فتصحّح جملة كاملة بضغطات متتالية.
- **تراجع خلال 30 ثانية**: من القائمة، أو باختصار تعيّنه، يعيد النص الأصلي.
- **الاتجاه تلقائي بالأغلبية**: أغلبية عربية تُحوَّل إلى إنجليزية، والعكس؛ وما
  كان من اللغة الأخرى يبقى كما هو.
- **تبديل مصدر الإدخال** بعد كل تحويل، لتكمل الكتابة باللغة الصحيحة مباشرة.
- **حسم مفتاح «لا»**: في تخطيط Arabic‑PC يفصل بدّل بين `b` و`gh` بقائمة من 45
  ألف كلمة إنجليزية مضمَّنة في التطبيق.
- **خرائط حيّة**: تُبنى من التخطيطين المفعّلين في macOS مباشرة، ومعها نسخ
  مضمَّنة احتياطية من Arabic (Mac) وArabic‑PC وABC.
- **حد التحديد**: 10 آلاف حرف.
- **سريع وخفيف**: 7–95ms للتحويل في معظم التطبيقات المختبرة (0.3–1.2 ثانية في
  Mail وFigma، عبر لوحة المفاتيح)، ونحو 15MB من الذاكرة في الخمول.
- **يعمل حيث لا يكشف التطبيق نصّه**: نص Figma على اللوحة، ومحررات Electron
  التي تقبل الكتابة عبر تسهيلات الاستخدام ثم تتجاهلها (يتحقق بدّل أن النص تغيّر
  فعلًا، وإلا لصقه).

### الواجهة

- **قائمة شريط قوائم** أصيلة بثلاث حالات («جاهز» و«متوقف مؤقتًا» و«تحتاج
  صلاحية»)، وأيقونة الشريط تتبع الحالة نفسها.
- **إشعار HUD** أصيل لا يأخذ التركيز من التطبيق الذي تكتب فيه، بحالات للتحويل
  والتراجع والحقل المحمي والتحديد الطويل جدًا وعدم وجود نص.
- **إعدادات بخمس لوحات**: عام، الاختصارات، التخطيطات، الاستثناءات، حول.
- **ترحيب بثلاث خطوات**: الفكرة، ثم الصلاحية ببطاقة تتحقق منها حيًّا، ثم حقل
  تجربة حقيقي يعمل بالمحرّك نفسه.
- **عربية أولًا كاملة RTL**، ونسخة إنجليزية كاملة، ووضعان فاتح وداكن يتبعان
  النظام، وخطّا Cairo وAlmarai مضمَّنان في الحزمة.

### الخصوصية والأمان

- **صلاحية واحدة فقط**: تسهيلات الاستخدام (Accessibility). لا يطلب بدّل مراقبة
  الإدخال (Input Monitoring).
- **لا نص يُحفظ أو يُسجَّل**، لا على القرص ولا في أي سجل.
- **الحافظة تعود كاملة** بعد كل تحويل، وما يمرّ فيها مؤقتًا يوسَم بأنه عابر
  وسرّي (transient/concealed).
- **الحقول المحمية لا تُمسّ**: حقول كلمات المرور، وكل ما يفعّل فيه macOS الإدخال
  الآمن. ولا يُعتمد على الإدخال الآمن وحده، فـSafari لا يفعّله في حقول كلمات المرور.
- **14 استثناءً افتراضيًا**: 7 طرفيات و7 من مديري كلمات المرور، قابلة للتعديل
  من الإعدادات ← الاستثناءات.

### التوزيع والتحديث

- **حزمة Universal** واحدة تعمل أصيلًا على Apple Silicon وIntel.
- **شهادة توقيع ذاتية ثابتة**: صلاحية Accessibility تبقى عبر التحديثات ولا
  تُطلب من جديد.
- **تحديث موقَّع تلقائي**: فحص يومي صامت، وعنصر «ثبّت التحديث» في القائمة،
  وحالة التحديث في الإعدادات؛ لا حوار مفاجئ.

إصدارات 0.9.x كانت بناءات داخلية أثناء التطوير ولم تُنشر؛ فهذا أول إصدار علني.
