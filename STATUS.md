# حالة التنفيذ — بدّل

> يُحدَّث في نهاية كل مرحلة. هذا هو المصدر الوحيد لمعرفة «أين وصلنا»، فاقرأه أول كل جلسة.

| المرحلة | العنوان | الحالة | التاريخ |
|---|---|---|---|
| 0 | تجهيز البيئة والهيكل | ✅ مكتملة | 2026-09-20 |
| 1 | النواة (Core) | ✅ مكتملة | 2026-09-20 |
| 2 | طبقة النظام | ✅ مكتملة | 2026-09-20 |
| 3 | الواجهة | ✅ مكتملة | 2026-09-21 |
| 4 | الصقل والاختبار اليدوي | ✅ مكتملة (بنود الجهاز في الطابور) | 2026-09-21 |
| 5 | الأيقونة والتوقيع والتحديث | ✅ مكتملة (بنود الجهاز في الطابور) | 2026-09-21 |
| 6 | الإطلاق | 🟡 الوثائق والأصول جاهزة، والنشر ينتظر الطابور وموافقتك | 2026-09-21 |
| 7 | الاختبار على الجهاز (عند الطلب) | ✅ نُفّذت، وبقي في الطابور 5 بنود (3 بعد النشر، و2 بعد إجابتك عن طلبَي النظام) | 2026-09-22 |

الرموز: ⬜ لم تبدأ · 🟡 جارية · ✅ مكتملة · ⛔ متوقفة على المستخدم

## طابور الاختبار على الجهاز

> يُنفَّذ في المرحلة 7 فقط، حين يكتب المستخدم «ابدأ المرحلة 7». الطريقة في EXECUTION.md. البند الذي ينجح يُحذف من هنا ويُسجَّل في `docs/test-matrix.md`.

| # | البند | من المرحلة | الأداة |
|---|---|---|---|
| 12 | تثبيت `release/0.9.1/Baddel_0.9.1_universal.dmg` على **حساب مستخدم نظيف**: تحذير Gatekeeper وطريقة الفتح الموثَّقة، ثم الترحيب، ثم منح الصلاحية، ثم تحويل ناجح. **إنشاء الحساب عليك** (يحتاج كلمة مرور المدير) | 5 | حساب اختبار في macOS |
| 13 | حوارات المحدِّث: «محدَّث» بعد النشر، وعنصر «ثبّت التحديث X…». وأن التنشيط يعود إلى التطبيق السابق بعد إغلاق الحوار (لم يُحكم عليه، فطلبا النظام المعلّقان أخذاه). «تعذّر» ✓ في المرحلة 7 | 5→7 | قراءة عناصر القائمة ونقرها |
| 14 | **اختبار التحديث:** ثبّت 0.9.1 (`release/0.9.1/`)، وامنح الصلاحية، ثم حدّث إلى 1.0.0 عبر المحدِّث بعد نشره. يجب أن تبقى الصلاحية ولا يظهر تحذير Gatekeeper. **يحتاج `latest.json` متاحًا للعموم**، فهو البند الوحيد الذي يُختبر بعد النشر (الخطوة 11 في `docs/launch/release-checklist.md`) | 5→6 | DMG 0.9.1 ثم «تحقّق من التحديثات…» |
| 15 | إعادة `run.sh` **بلا** `BADDEL_TRUST_FRONT` بعد إجابتك عن طلبَي النظام، ليُرى البناء النهائي في الحال الطبيعية أيضًا (مرّ كله في الحال المعلّقة) | 7 | `run.sh` |
| 16 | لقطتا القائمة بالوضع الفاتح (`menu-ar-light.png` و`menu-en-light.png`)، حين يكون مظهر النظام فاتحًا | 3→7 | `docs/test-matrix.md` ← «حالات أخرى» |

## قرارات معلّقة على المستخدم

- [x] **التوقيع:** حُسم 2026-09-21: **شهادة ذاتية ثابتة** (لا اشتراك Developer ID). التفاصيل في `docs/signing.md`.
- [x] **مستودع GitHub:** أُنشئ خاصًّا: https://github.com/iSltanX/Baddel
- [x] **الأيقونة:** اعتُمدت 2026-09-21 (كبستان A/ع، مصدرها صفحة `03 · App Icon` في Figma، انظر `design/icon/README.md`).

## سجل المراحل

_(يُضاف هنا قسم لكل مرحلة عند تنفيذها: ما أُنجز · مخرجات معيار القبول · ما تعذّر · ملاحظات لمراحل لاحقة)_

### ما قبل المرحلة 0 — 2026-09-20
- أُنشئ المجلد ونُقلت إليه وثائق التخطيط: خطة التنفيذ التفصيلية وبرومبتا Figma.
- بيئة الجهاز الحالي: macOS 27.0 · Xcode 27.0 · Rust 1.96.1 · Node عبر nvm (v20 وv24) · arm64.
- **قرار تقني:** التحويل من Swift إلى **Tauri 2 (Rust + Svelte)** انسجامًا مع Luma ونفّذ. أُلغيت مرحلة Figma، فالواجهة تُصمَّم وتُراجَع مباشرة HTML. الأسباب والتكاليف في الخطة التفصيلية §2.
- ملاحظة بيئة: على هذا الجهاز `node` و`npm` دوال صدفة كسولة لـnvm تنهار في الصدفة غير التفاعلية. الحل في `scripts/env.sh`.

### المرحلة 0 — تجهيز البيئة والهيكل — ✅ 2026-09-20

**البيئة:** macOS 27.0 · Xcode 27.0 · Rust 1.96.1 (الهدفان arm64 وx86_64 مثبّتان) · Node 24.16.0 · npm 11.13.0.

**ما أُنجز**
- `git init` (الفرع `main`) و`.gitignore` ورخصة MIT.
- Cargo workspace: `crates/baddel-core` (Rust صِرف، فيه `is_arabic` واختباره) و`src-tauri`.
- واجهة Svelte 5 + TypeScript + Vite 7 (صفحة شكلية) مع `svelte-check`.
- تطبيق Tauri 2: بلا نوافذ عند الإقلاع، و`ActivationPolicy::Accessory` مع `LSUIElement` في `Info.plist`، وأيقونة tray بصيغة template، وقائمة فيها «إنهاء بدّل» (⌘Q). إغلاق آخر نافذة لا ينهي التطبيق (`prevent_exit` إلا عند الخروج الصريح).
- الإضافات المسجَّلة: `global-shortcut` و`autostart` و`store` و`process`. CSP مقيَّدة، و`macOSPrivateApi` مغلق.
- `scripts/env.sh` و`scripts/check.sh`.
- أيقونات مؤقتة: glyph الـtray (كبستان وسهم) وأيقونة تطبيق بلا حروف، مولَّدتان بسكربت. المصدر في `design/placeholder/`.

**مخرجات معيار القبول**
- `./scripts/check.sh` ← exit 0: اختبار واحد يمر · clippy بلا تحذيرات · svelte-check بلا أخطاء ولا تحذيرات · بناء الواجهة ناجح.
- `npm run tauri build -- --debug` ← `target/debug/bundle/macos/Baddel.app`.
- التشغيل الفعلي: الأيقونة ظهرت في شريط القوائم (لقطة شاشة) · العملية `background only = true` وبلا نوافذ (لا أيقونة Dock) · فتح القائمة والنقر على «إنهاء بدّل» أنهى العملية.

**انحرافات عن الخطة**
- `tauri-plugin-updater` **مربوط في Cargo.toml وغير مسجَّل**: الإضافة ترفض الإقلاع بلا مفتاح توقيع عام في الإعدادات. يُسجَّل في المرحلة 5 مع توليد المفاتيح.
- `tauri-plugin-dialog` لم يُضف بعد (مخطط للمرحلة 3).

**ملاحظات لمراحل لاحقة**
- npm 11 يحجب سكربت postinstall لـ`esbuild` افتراضيًا. البناء يعمل بدونه، فلا إجراء مطلوب.
- المرحلة 3: النوافذ تُنشأ بالمعرّفين `settings` و`onboarding`، والصلاحيات في `capabilities/default.json` مربوطة بهما مسبقًا.
- المرحلة 5: `bundle.targets` حاليًا `["app"]` فقط، ويُضاف `dmg` هناك.

**معلّق على المستخدم:** قرار إنشاء مستودع GitHub، وأول commit.

### المرحلة 1 — النواة (Core) — ✅ 2026-09-20

**ما أُنجز** (كله في `crates/baddel-core`، Rust صِرف بلا اعتماديات)
- `static_layouts.rs` **مولَّد من نظام macOS نفسه**: `scripts/dump-layouts.swift` يستخرج عبر TIS و`UCKeyTranslate` مخرجات المفاتيح 0…50 بالطبقات الأربع، و`scripts/gen-static-layouts.py` يكتب الجداول. التخطيطات المضمَّنة: ABC وArabic (Mac) وArabic‑PC.
- `layout.rs`: `Layer` و`LayoutProvider` و`StaticLayout` و`LayoutMap::build`، ومعها الاختصاران `arabic_mac()` و`arabic_pc()`. لا تُربط المفاتيح المتطابقة المخرجات ولا مفاتيح المسافات والتحكم.
- `convert.rs`: `detect_direction` (بالأغلبية، والتعادل يُحسب عربيًا، و`None` إذا خلا النص من الحروف)، و`convert` و`convert_as`. المطابقة بالأطول أولًا، كلمةً بكلمة، مع حفظ المسافات البيضاء حرفيًا.
- `disambiguate.rs`: لكل مفتاح متعدد المحارف قراءة بديلة (حروفه مفردةً). تُجرَّب القراءات (حتى 2⁶) على قائمة الكلمات، وأول كلمة حقيقية تفوز، والافتراضي المفتاح المفرد (`b`).
- `assets/en-words.txt`: 45,104 كلمة من SCOWL 2020.12.07 (المقاسات 10 و20 و35 و40، حجمها 409KB)، برخصة متساهلة نصها الكامل في `THIRD_PARTY.md`.

**مخرجات معيار القبول**
- `cargo test -p baddel-core` ← **48 اختبارًا تمر**: 44 سلوكيًا، و2 لجدول الحالات (60 حالة)، واختبار وحدة، واختبار توثيق.
- `cargo clippy -p baddel-core --all-targets -- -D warnings` ← نظيف.
- `./scripts/check.sh` ← exit 0.

**الوكلاء (2 بالتوازي، sonnet)**
- أ (قائمة الكلمات): رُوجعت مخرجاته. الرخصة مقروءة ومنقولة نصًا، وللقائمة اختبار يتحقق من الترتيب والنظافة والحجم.
- ب (60 حالة مشتقة يدويًا من جداول التخطيط، ومتحقَّق منها بسكربت مستقل): مرّت الحالات الستون من أول تشغيل دون أي تعديل، وهذا تحقق مستقل من صحة المحوِّل.

**اكتشافات وانحرافات عن الخطة**
- **التخطيط المفعّل عند المستخدم هو «Arabic» (Mac) لا «Arabic‑PC».** الصف السفلي في التخطيطين مختلف كليًا، و**تخطيط Mac لا يحوي مفتاح «لا»** (`b` يكتب «ز»). فالتباس `b`/`gh` يخص PC وحده، وعلى Mac «لا» تعني `gh` دائمًا. المحوِّل عام: ينشأ البديل من الخريطة نفسها، لا من افتراض مسبق.
- الاحتياطي الافتراضي صار **Arabic (Mac)** بدل Arabic‑PC، والاثنان مضمَّنان ومختبَران.
- **طبقتا Option مستبعدتان من الربط** (`Layer::PAIRED` = الأساسية وShift). ربطهما كان يحوّل «…» إلى «گ» و«€» إلى «›»، أي يفسد نصًا صحيحًا، ولا أحد يكتب الإنجليزية مع ضغط Option. الطبقتان باقيتان في `Layer::ALL` و`LayoutProvider` لمعاينة لوحة المفاتيح في المرحلة 3.
- قائمة الكلمات 45 ألفًا لا 10 آلاف، لأن الصيغ المصرَّفة (`lights` و`brighter`) لازمة لدقة الحسم. البحث ثنائي على شرائح من النص المضمَّن، بلا نسخ.
- التحويل وفيّ للمفاتيح حتى في الرموز: «)اثممخ(» ← `(hello)`، و«]» على PC ← `D`. هذا مقصود.

**ملاحظات لمراحل لاحقة**
- المرحلة 2: المزوّد الحي يطبّق `LayoutProvider` ويُمرَّر إلى `LayoutMap::build(&arabic, &latin)`. إن عادت الخريطة فارغة (`is_empty()`) فالسقوط إلى `LayoutMap::arabic_mac()`. معرّف Arabic‑PC في النظام هو `com.apple.keylayout.ArabicPC` (بلا شرطة).
- المرحلة 2: `convert()` يعيد `None` للنص الخالي من الحروف (أرقام ورموز فقط). عندها يُستخدم `convert_as` باتجاه **معاكس لمصدر الإدخال الحالي**.
- المرحلة 3: معاينة لوحة المفاتيح تُبنى من `LayoutProvider::output` مباشرة (الطبقتان الأساسية وShift)، ويُظلَّل مفتاح «لا» **فقط إن وُجد مفتاح متعدد المحارف** في التخطيط المختار. نص المواصفة عن «مفتاح B» يصح على PC وحده.
- حالة معروفة: كلمة على PC فيها «لا» وقراءتاها كلمتان صحيحتان تُحسم لـ`b`. لم تظهر لها حالة عملية.

### المرحلة 2 — طبقة النظام — ✅ 2026-09-20

**ما أُنجز** (يُبنى، وclippy نظيف)
- `src-tauri/src/sys/`: `carbon.rs` (روابط TIS و`UCKeyTranslate` وSecure Input) · `input_source.rs` (التخطيطات الحية + التبديل، **على الخيط الرئيسي فقط** عبر `sys::on_main`) · `text_access.rs` (AX: النص المحدَّد، و`selected_range` و`select_range` و`string_for_range`) · `keysynth.rs` · `pasteboard.rs` (لقطة واستعادة كاملتان + وسما Transient وConcealed) · `permissions.rs` · `frontmost.rs`.
- `controller.rs` (خيط خاص): الحراسة (إيقاف مؤقت / صلاحية / Secure Input / استثناءات) ← التحديد الصريح ← المدّ ← آخر كلمة. التراجع من الذاكرة. لا يُسجَّل أي نص، و`trace!` في بناء debug يطبع المسار فقط.
- **مساران لآخر كلمة:**
  1. **نطاق النص عبر AX** (المفضَّل): موضع المؤشر ← قراءة ما قبله ← تحديد الكلمة بإزاحات منطقية UTF‑16 ← استبدال ← إعادة المؤشر. لا يتأثر باتجاه النص (RTL أو مختلط) ولا بعلامات الترقيم، وسريع (15–60ms).
  2. **المفاتيح** (للتطبيقات بلا AX): ⌘⇧سهم لقراءة السطر ← اختيار الذيل ← تحديده حرفًا حرفًا بـ⇧سهم ← لصق. استُبدل به ⌥⇧سهم، لأن العربية المكتوبة خطأً مليئة بـ`;` و`,` و`[` و`'` التي تعدّها مفاتيح الكلمات فواصل.
- `state.rs`: إعدادات في الذاكرة مع الاستثناءات الافتراضية. `tray.rs`: الحالة، «تراجع عن آخر تحويل»، «امنح الصلاحية…»، «إنهاء».
- `scripts/dev-build.sh`: يبني حزمة debug ويوقّعها بأول شهادة «Apple Development» أو «Developer ID» في السلسلة (أو `$BADDEL_SIGN_ID`). **تحقق فعليًا: صلاحية Accessibility بقيت بعد أربع إعادات بناء.**

**نتائج الاختبار الآلي** (osascript يضبط النص ويطلق ⌥⇧Space، و`/tmp` يحوي أدوات الاختبار ولا تُحفظ في المستودع)
| التطبيق | المسار | تحديد | آخر كلمة | مدّ | الحافظة تُستعاد | تبديل اللغة |
|---|---|---|---|---|---|---|
| TextEdit | نطاق النص AX | ✅ | ✅ (عربي ولاتيني، ومع مسافة بعد الكلمة، ومع عدم تطابق لغة اللوحة) | ✅ (حتى 3 كلمات، وفي نص مختلط الاتجاه) | ✅ | ✅ |
| Safari (textarea) | نطاق النص AX + لصق | — | ✅ | ✅ | ✅ | ✅ |
| Brave (textarea) | انظر «الإغلاق» أدناه | — | ✅ | ✅ | ✅ | ✅ |

**تحديث لاحق في اليوم نفسه (كود فقط، بلا اختبار آلي)**
- `text_access::enable_accessibility(pid)`: عند غياب العنصر المركَّز (Chromium/Electron) يُضبط `AXManualAccessibility`، ثم `AXEnhancedUserInterface` على عنصر التطبيق **مرة واحدة لكل pid**. يُنتظر حتى 600ms لبناء الشجرة، فيعمل مسار نطاق النص هناك أيضًا. أثر جانبي معروف لـ`AXEnhancedUserInterface` في Chrome: بطء طفيف وتعارض محتمل مع أدوات ترتيب النوافذ. يُقيَّم في المرحلة 4.
- `trace!` إضافي في مسار المفاتيح: أطوال فقط، بلا نصوص.
- الأيقونة: رفض المستخدم أيقونة Stitch والمفاهيم الثلاثة في `design/icon/concepts/` («مو حلوات»)، وأُجّل الموضوع. الأيقونة الحالية في الحزمة مؤقتة (`scripts/gen-icon.py`).

**الإغلاق — معيار القبول** (بإذن المستخدم الصريح بالاختبار الآلي لأنه بعيد عن الجهاز)
| البند | النتيجة |
|---|---|
| TextEdit — تحديد · آخر كلمة · مدّ | ✅ مسار نطاق النص، 15–60ms |
| Safari (حقل ويب) — آخر كلمة · مدّ | ✅ مسار نطاق النص + لصق، نحو 260ms |
| Brave (حقل ويب) — آخر كلمة · مدّ | ✅ أول ضغطة بعد تشغيل Brave تمر بمسار المفاتيح (نحو 1.8 ثانية تشمل مهلة الإيقاظ)، وما بعدها بمسار نطاق النص (نحو 250ms) |
| مسار المفاتيح (مفروضًا بـ`BADDEL_FORCE_KEYS=1` في TextEdit) — عربي · لاتيني فيه `;` · مدّ في نص مختلط الاتجاه | ✅ نحو 600ms |
| الحافظة تطابق ما قبلها — نص | ✅ في كل التجارب |
| الحافظة تطابق ما قبلها — **صورة** (PNG بكل تمثيلاتها، و`clipboard info` متطابق قبل وبعد) | ✅ |
| حقل آمن أصيل (`display dialog … with hidden answer`) | ✅ `Blocked` خلال 41µs، والنص والحافظة لم يُمسّا |
| تبديل لغة لوحة المفاتيح بعد التحويل | ✅ في الاتجاهين |
| `./scripts/check.sh` و`cargo clippy --workspace -- -D warnings` | ✅ |

- VS Code غير مثبّت. يحلّ محله مسار المفاتيح المفروض + Brave البارد. اختبار تطبيق Electron حقيقي وLuma مؤجَّل إلى مصفوفة المرحلة 4.
- **إعادة تصميم مسار المفاتيح:** لم يعد يحدّد الكلمة. صار يحدّد **من حافة السطر إلى المؤشر** (⌘⇧سهم)، ويقرأه، ثم يلصق السطر وقد استُبدل ذيله. السبب أن التحديد بالكلمة ينكسر عند `;` و`,` و`[`، والمشي حرفًا حرفًا يضلّ في النص مختلط الاتجاه. الثمن: في محرر نص منسَّق يفقد ذلك الجزء من السطر تنسيقه. هذا مقبول لأن هذا المسار لا يعمل إلا في التطبيقات التي لا تكشف نطاق النص.
- Chromium يردّ بخطأ على طلب تفعيل AX وينفّذه مع ذلك، فلا يُعتمد على الرد بل تُنتظر الشجرة. 600ms لا تكفي Brave البارد، فتمر أول ضغطة بمسار المفاتيح.

**ملاحظات لمراحل لاحقة**
- المرحلة 4: حادثة أثناء الاختبار: صفحة اختبار كلمة المرور في Safari لم تأخذ التركيز، فحُوِّلت آخر «كلمة» في **شريط العنوان**. لا ضرر، لكنها تذكير بأن شريط العنوان وحقول البحث أهداف مشروعة، وتحويل نص ليس «كلمة مكتوبة خطأً» وارد. يُدرس ألا يُحوَّل إلا ما يحوي حروفًا فعلًا وكانت نتيجته من النص الآخر بالكامل.
- المرحلة 4: ضبط المهلات (مسار المفاتيح نحو 600ms، ومعظمها `PASTE_SETTLE` ومهلة النسخ).
- المرحلة 3: الحدث `conversion` يحمل `Outcome` فقط. HUD يحتاج النصين، فيُمرَّران في الذاكرة إلى HUD الأصيل مباشرة لا عبر حدث ويب.

**ملاحظات**
- ⚠️ الاختبار الآلي يطلق ضغطات مفاتيح حقيقية في التطبيق الأمامي. لا يُجرى إلا **بإذن صريح من المستخدم في الجلسة نفسها** (انزعج منه مرة، ثم أذن به حين ابتعد عن الجهاز).
- تبديل مصدر الإدخال من عملية أخرى (أداة الاختبار) يصل إلى تطبيقنا بتأخير، ولذلك كان `expected` خاطئًا أحيانًا في الاختبار. المسار لا يعتمد عليه إلا عند نص بلا حروف.
- معرّف مصدر الإدخال العربي عند المستخدم: `com.apple.keylayout.Arabic`.
- المرحلة 5: شهادة «Apple Development» موجودة على الجهاز، وهي للتطوير المحلي فقط، والتوزيع يحتاج «Developer ID Application».

### المرحلة 3 — الواجهة — ✅ 2026-09-21 (نُفّذت 2026-09-20، وأُغلقت بعد مراجعة المستخدم وتطبيق تصميم v2)

**البيئة:** macOS 27.0 · Xcode 27.0 · Rust 1.96.1 · Node 24.18.1 · npm 11.16.0.

**ما أُنجز**

_الأساس البصري_
- `src/lib/theme.css`: الألوان الاثنتان والعشرون من المواصفة بوضعَي `prefers-color-scheme`، مع رمزين مشتقّين: `--accent-on` (لون النص فوق سطح مصمت بلون التمييز، ينقلب في الوضع الداكن لأن لون التمييز نفسه ينقلب) و`--keycap-edge`. سلّم الخطوط والمسافات وأنصاف الأقطار وحلقة التركيز.
- الخطوط محلية بالكامل (`@font-face` من `src/assets/fonts/`)، **ولا طلب شبكي واحد**: تحقّقت من حزمة الإنتاج فلا تحوي إلا اسم مساحة XML ورابط المستودع (يُفتح في متصفح المستخدم) وروابط رسائل أخطاء Svelte كنصوص.

_الواجهة (Svelte 5 runes)_
- `src/lib/state.svelte.ts`: الجسر الوحيد إلى Rust. لا حالة في الصفحات: تقرأ وتكتب عبر `invoke` وتستمع لحدثَي `settings` و`permission`، فتبقى النافذتان والقائمة متطابقة.
- المكوّنات: `Keycap` · `ShortcutRecorder` · `FormRow` · `GroupCard` · `PermissionCard` · `ConversionSample` · `AppListRow` · `Button` · `PaneToolbar` · `Switch` · `Select` · `Icon` (أيقونات خطية 1.5 على نمط SF Symbols في `src/lib/icons.ts`).
- الإعدادات (560pt، شريط أقسام علوي): الأقسام الخمسة، وعنوان النافذة يتبع القسم، وارتفاع النافذة يتبع محتوى القسم، وآخر قسم يُستعاد عند الفتح.
- الترحيب (560×600، ثلاث خطوات) بنصوص المواصفة وبطاقة الصلاحية الحية وحقل تجربة يعمل فعلًا.
- `src/lib/mock.ts`: بدائل أوامر Rust لمراجعة الشاشات في المتصفح (`npm run dev`) كما تنصّ الخطة. تُحمَّل فقط في بناء تطوير خارج Tauri، **ومحذوفة من حزمة الإنتاج** (تحقّقت: لا أثر لها في `dist`).

_طبقة Rust_
- `settings.rs`: كل التفضيلات في مكان واحد، محفوظة عبر `tauri-plugin-store`. متجر تالف لا يمنع الإقلاع (سقوط إلى الافتراضيات).
- `commands.rs`: 19 أمرًا. `sync.rs` يطبّق أي تغيير على الجميع دفعةً واحدة (autostart، إعادة ربط الاختصارات، بناء القائمة، بثّ الحدث).
- `shortcuts.rs`: الاختصارات الثلاثة. الرفض من النظام يعني **تعارض**، وعندها يُعاد الاختصار السابق بدل ترك المستخدم بلا اختصار.
- `windows.rs`: النافذتان تُنشآن عند الطلب وتُدمَّران عند الإغلاق، وتُعرضان بعد أن ترسم الصفحة نفسها (لا وميض)، وسياسة التنشيط تعود إلى `Accessory` عند إغلاق آخر نافذة.
- `tray.rs`: القائمة كاملة بحالاتها الثلاث، وآخر تحويل مع «تراجع»، وعنصر ✓، و⌘, و⌘Q. تُبنى دائمًا على الخيط الرئيسي.
- `hud.rs`: `NSPanel` أصيل غير منشِّط يتجاهل الفأرة، بخمس حالات، وحركة 120ms/900ms/200ms تحترم Reduce Motion. خط Almarai يُبنى من ملف الخط المضمَّن مباشرة عبر CoreText، **بلا تسجيل في النظام**.
- `sys/app_icons.rs` (أيقونات التطبيقات) · `sys/chrome.rs` (ارتفاع شريط العنوان) · `sys/sound.rs` (الصوت الخافت).

**مخرجات معيار القبول**

| البند | النتيجة |
|---|---|
| `./scripts/check.sh` (يشمل `svelte-check`) | ✅ exit 0، بلا أخطاء ولا تحذيرات |
| `cargo clippy --workspace --all-targets -- -D warnings` | ✅ نظيف |
| الإعدادات تبقى بعد إعادة التشغيل | ✅ تحقّقت بثلاثة حقول (اللغة، الصوت، اختصار التراجع) |
| بعد إغلاق الإعدادات: لا عملية WebKit | ⚠️ عملية العرض (`WebContent`) وعملية `GPU` تختفيان فورًا، لكن `WebKit.Networking` (نحو 15MB) تبقى لحظة ثم تنسحب. تُقاس في المرحلة 4 على بناء release |
| ظهور HUD لا يسحب التركيز | ✅ **اختُبر 2026-09-21 فكُشف خلل ثم أُصلح** — التفاصيل في «إغلاق المرحلة 3» آخر الملف |
| لقطات كل شاشة × (عربي/إنجليزي) × (فاتح/داكن) | ✅ 42 لقطة في `docs/screenshots/` من البناء الحالي، **وراجعها المستخدم وجرّب التطبيق بنفسه** (2026-09-21) |

**الوكلاء (2 بالتوازي، sonnet)**
- أ (النصوص): 99 مفتاحًا في `ar.json` و`en.json` بمفاتيح متطابقة. راجعتها: النصوص العربية منقولة حرفيًا عن المواصفة. صحّحت سهم HUD في الإنجليزية إلى `→`، وأضفت `settings.about.showWelcome`. امتنع الوكيل عن اختلاق عنوان لقسم التخطيطات لأن المواصفة لا تعطيه — قرار صحيح، وتُرك القسم بلا عنوان مخترع.
- ب (الخطوط): عشرة ملفات في `src/assets/fonts/`. راجعتها: الأوزان 600/700 لـCairo و400/700 لـAlmarai، وتغطية الحروف العربية كاملة، ونصّا رخصتَي OFL الأصليان. Cairo مستخرج من الخط المتغيّر بـ`fonttools`.

**انحرافات عن المواصفة وأسبابها**
- **HUD كبسولة مرسومة لا `NSVisualEffectView`.** قناع الصورة على طبقة الزجاج كان يتمدّد فيخرج شكل مشوّه، وتدوير الزوايا مباشرةً يحتاج اعتمادية Core Animation جديدة. الكبسولة تُرسم بتعبئة `rgba(20,21,24,.94)` وحدّ `rgba(255,255,255,.08)` بدقة مضاعفة — وهي أقرب إلى المرجع البصري (الملف ب) أصلًا. الثمن: لا ضبابية خلفية.
- **نص HUD أبيض موحَّد** لا 40٪ للطرف المصدر كما في مرجع Make: 40٪ أبيض على كبسولة داكنة يسقط دون 4.5:1، والمواصفة تشترط 4.5:1.
- **أزرار النافذة (الأحمر/الأصفر/الأخضر) تظهر على اليمين على جهازك** لأن لغة نظامك عربية (`ar-SA`) فيعكسها macOS بنفسه. نحن لا نعكسها ولا نرسمها. ولذلك نُقل زر الرجوع في الترحيب من شريط العنوان إلى أعلى المحتوى، فلا يصطدم بها في أي من الحالتين.
- **«تحقّق من التحديثات…» في القائمة و«تحقق الآن» في الإعدادات معطَّلان** حتى المرحلة 5 (المحدِّث ومفتاح التوقيع).
- **حقل التجربة في الترحيب بلا زر.** المواصفة تعلّم بالممارسة: المؤشر في الحقل ثم الاختصار الحقيقي. النجاح يُلتقط من حدث `conversion`، ومفتاح Return بديل لمن لم يُربط اختصاره بعد.
- **الاستثناءات:** تطبيق غير مثبَّت يُعرض بمعرّفه مع أيقونة عامة، ولا يُحذف من القائمة.
- **ارتفاع نافذة الإعدادات:** Tauri يعطي الرقم نفسه للحجم الداخلي والخارجي على macOS، فكان المحتوى يفقد ارتفاع شريط العنوان. يُقاس الشريط فعليًا من AppKit في `sys/chrome.rs` بدل ثابت مخمَّن. والنافذة غير القابلة للتحجيم ترفض التحجيم برمجيًا، فيُرفع القيد للحظة الضبط.

**أدوات مضافة (بناء debug فقط، على نمط `BADDEL_FORCE_KEYS` من المرحلة 2)**
- `BADDEL_THEME=light|dark` · `BADDEL_WINDOW=settings[:قسم]|onboarding[:خطوة]|hud[:حالة]` · `BADDEL_HUD_CAPTURE=<مسار>` · `BADDEL_CLOSE_AFTER=<ثوانٍ>`.
- `scripts/capture.sh` يلتقط كل اللقطات، و`scripts/screenshot.sh` و`scripts/window-id.swift` و`scripts/crop.swift` تخدمه.
- لوح HUD لا يقبل `screencapture` (لوح عائم غير منشِّط)، فيرسم نفسه إلى PNG من داخل التطبيق — صورة أمينة لما يُرسم فعلًا.

**ما تعذّر**
- **لقطة قائمة شريط القوائم غير موجودة**: فتح القائمة الأصيلة يحتاج نقرًا مركَّبًا على أيقونة الشريط، وهو إدخال صناعي لم يُستأذن فيه.
- **اختبار «HUD لا يسحب التركيز»** لم يُنفَّذ للسبب نفسه.

**ملاحظات على الالتقاط**
- قفل الشاشة يمنع `screencapture` من التقاط النوافذ، فـ`capture.sh` لا يعمل إلا والشاشة مفتوحة.
- كان في السكربت سباق توقيت: نافذة التشغيل السابق تبقى لحظة بعد إشارة الإنهاء فتُلتقط بدل الجديدة (ظهرت لقطة الخطوة 2 وفيها الخطوة 3). صار ينتظر اختفاء العملية فعليًا.

**المطلوب منك**
1. راجع `docs/screenshots/` (42 لقطة) واحكم على الشكل.
2. إن أذنت بإدخال صناعي على جهازك: فتح قائمة شريط القوائم والتقاطها، واختبار أن HUD لا يسحب التركيز من TextEdit.

**ملاحظات لمراحل لاحقة**
- المرحلة 4: `WebKit.Networking` تبقى نحو 15MB بعد إغلاق النافذة ثم تنسحب — تُقاس مع هدف «أقل من 40MB في الخمول» على بناء release.
- المرحلة 4: ذاكرة بناء debug بعد فتح نافذة كانت 111MB (بناء debug، غير ممثِّل).
- المرحلة 4: مراجعة الترجمة (الوكيل ب في تلك المرحلة) يجب أن تشمل جدول `menu_text.rs` في Rust — فهو نسخة يدوية موازية لمفاتيح `menu.*` و`hud.*`.
- المرحلة 5: ~~أيقونة `src/assets/app-icon.png` مؤقتة~~ (حُسمت 2026-09-21، انظر آخر الملف). ويبقى تسجيل `tauri-plugin-updater` وتفعيل عنصري التحديث المعطَّلين.

### تصميم v2 في Figma (خارج المراحل) — 2026-09-21

الملف المعتمد الوحيد: https://www.figma.com/design/le5J63MNuS9wV7kN8tYpsX/Badeel (الروابط السابقة حُذفت من `docs/design-reference.md`).
صفحاته: `00 Cover` · `01 Foundations` · `02 Components` · `03 App Icon` · `04 Menu Bar & HUD` · `05 Onboarding` · `06 Settings` · `Archive · v1 draft` (المسودة القديمة محفوظة دون حذف).
كل الشاشات بوضعين (Light/Dark) من مجموعة متغيرات واحدة `Color`، و11 مجموعة مكوّنات بحالاتها، وأيقونة مبنية على شبكة (Master 1024 + نسخة ≤32 + رمز شريط القوائم بثلاث حالات).

**طُبّق التصميم في الكود — 2026-09-21** (بعد اعتماد المستخدم للإضافات). القيم منقولة من Figma كما هي، والأيقونة **مُصدَّرة** من Figma لا مرسومة من جديد.
- `theme.css`: رموز `Color` و`Metrics` كاملة بالوضعين (`--bg-chrome` · `--border-strong` · `--text-tertiary` · `--accent-hover/pressed` · `--state-*-soft` · `--state-warning-text` · `--keycap-top` · `--focus-ring`…)، وحلقة تركيز 3px، و`unicode-range` عربي على Almarai وCairo فتُرسم اللاتينية بخط النظام.
- `icons.ts`: مسارات أيقونات Figma نفسها (36)، وسماكة الخط ثابتة 1.5px من 18px فأعلى.
- المكوّنات: `PaneToolbar` (80×52، محدَّد هادئ) · `Switch` 36×20 · `Button` (حالات + loading) · `Select` بعرض عمود التحكم 184 · `Keycap` · `ShortcutRecorder` (المسح داخل الحقل) · `PermissionCard` (عنوان + وصف، Missing كهرماني) · `GroupCard` (عنوان/trailing/footnote) · `FormRow` (فاصل بإزاحة) · `AppListRow` · `ConversionSample` · **جديد:** `Badge`.
- اللوحات: «عام» بعناوين المجموعات وبطاقة الصلاحية أعلى اللوحة عند نقصها، «الاختصارات» مع «استعد الافتراضي» ورسالة التعارض في وصف الصف، «التخطيطات» بشارة في رأس المجموعة وخريطة مفاتيح جديدة وحالة «لا تخطيط عربي»، «الاستثناءات» بحالة فارغة، «حول» بصفوف روابط وبطاقة «من الصانع نفسه» وكتلة الحقوق (سلطان + المستودع).
- الترحيب: فتحة Hero ثابتة، و«السابق» في التذييل، وبطاقة الاختصار + العيّنتين، وحقل التجربة الكبير بحالة النجاح. **تغيير سلوك:** «التالي» يتقدّم دائمًا، وزر منح الصلاحية صار في البطاقة فقط.
- i18n: 25 مفتاحًا جديدًا في `ar.json` و`en.json` (تطابق المفاتيح مُتحقَّق منه: 123 = 123). `settings.about.license` صار «الرخصة» والقيمة MIT تُعرض منفصلة.
- الأصول: `src/assets/app-icon.png` (512) و`design/icon/icon-macos-1024.png` و`src-tauri/icons/{32x32,128x128,128x128@2x,icon.png,icon.icns,icon.ico}` من تصدير Figma عبر `tauri icon`، و`tray.png` من رمز Idle (32px داخل 36).

- نافذة الترحيب: شريط العنوان صار `Overlay` مع `hidden_title` فتمتد الصفحة تحته (لون واحد، والنافذة 560×600 تمامًا)، والشريط العلوي في الصفحة `data-tauri-drag-region`. أضيفت لذلك صلاحية `core:window:allow-start-dragging` في `capabilities/default.json`.

**التحقق:** لقطات التطبيق الفعلي أُعيد التقاطها بعد التطبيق (`./scripts/dev-build.sh && ./scripts/capture.sh`: 42/42 ✓، منها 32 تغيّرت و10 لقطات HUD كما هي لأنه أصيل)، ورُوجعت مقابل Figma. وملف إعدادات المستخدم نُسخ احتياطيًا قبل الالتقاط وأُعيد بعده. `./scripts/check.sh` ✅ (اختبارات Rust 48 ناجحة، clippy نظيف، `svelte-check` 0 أخطاء و0 تحذيرات، `vite build` ناجح). ومراجعة بصرية في المتصفح عبر `mock.ts` للوحات الخمس وخطوات الترحيب الثلاث بالوضعين — مطابقة للوحات Figma.

**لم يُنفَّذ / يُطلب منك:**
- «آخر تحقق» في صف التحديثات ينتظر المحدِّث (المرحلة 5)، فيُعرض الإصدار وحده الآن.
- حجم اللاتيني داخل النص العربي: التصميم ينقصه 1px، وCSS لا يتيح ذلك لكل نظام كتابة دون خط وسيط — تُرك بالحجم نفسه.

**تعديلات لاحقة بعد تجربة المستخدم للتطبيق — 2026-09-21**
- «الاستثناءات»: الاسم اللاتيني كان يُحاذى يسارًا بعيدًا عن الأيقونة في العربية (عنصر `bdi` يأخذ اتجاهه الخاص). صار يلتصق ببداية الصف مع بقاء عزل الاتجاه (`AppListRow.svelte`)، وأُعيد التقاط لقطتي `settings-exceptions-ar-*`.
- أيقونة شريط القوائم تتبدّل حسب الحالة (`tray.rs`): جاهز · متوقف · يحتاج صلاحية، بالأولوية نفسها التي يستخدمها رأس القائمة. الصور الثلاث template مُصدَّرة من Figma (`icons/tray*.png`)، ولا تُستبدل الصورة إلا عند تغيّر الحالة فعلًا (مقارنة `AtomicU8`) داخل `rebuild` القائم — لا مؤقّت ولا استطلاع جديد، فلا أثر على السرعة أو الذاكرة. تُحقِّق منها بلقطة لشريط القوائم في حالتي «جاهز» و«متوقف».
- نافذة «أضف تطبيقًا…» (NSOpenPanel) كانت تظهر بالإنجليزية على نظام عربي لأن الحزمة لا تُعلن لغاتها. أضيف `CFBundleLocalizations = [ar, en]` و`CFBundleAllowMixedLocalizations` في `src-tauri/Info.plist`؛ تُحقِّق أن macOS يرى اللغتين ويختار `ar` لتفضيل `ar-SA`. ملاحظة: لغة نوافذ النظام تتبع لغة macOS لا خيار «لغة الواجهة» داخل بدّل.
- **ملفات الأيقونة حُسمت ولم تُؤجَّل:** `scripts/gen-icon.py` حُذف (كان سيرسم الأيقونة القديمة فوق المعتمدة عند أي تشغيل)، وطبقات `design/icon/*.svg` و`icon-flat.svg` و`preview.png` أُعيد تصديرها من Figma (إطار `Export / Icon Composer layers` الجديد: الـMaster مكبَّرًا 1024/824، بلا ظلال، والحروف outlines). أضيف `design/icon/README.md` يوثّق المصدر والهندسة وأمر إعادة توليد أيقونات الحزمة، وحُدّثت الخطوة 1 من المرحلة 5 في `EXECUTION.md`.
- ملف Stitch القديم: حُذفت منه الصفحة الفارغة ومجموعتا المتغيرات اللتان أُنشئتا فيه بالخطأ (بالمعرّفات نفسها بعد التحقق)، وعاد كما كان.
- أكّد المستخدم بالتجربة: نافذة «أضف تطبيقًا…» تظهر بالعربية، وسحب نافذة الترحيب يعمل.

### إغلاق المرحلة 3 — 2026-09-21

أُغلقت بموافقة المستخدم بعد تجربته التطبيق. قبل التعليم اختُبر البند الوحيد الذي لم يكن قد اختُبر قط، **فكشف خللًا حقيقيًا**:

**الخلل:** أول ظهور لـHUD في الجلسة كان يجعل بدّل التطبيق الأمامي (يسحب التركيز من التطبيق الذي يُكتب فيه)، بعد نحو ثانية من ظهوره. الإظهارات اللاحقة سليمة.
**السبب:** مكتبة tao تستدعي `activateIgnoringOtherApps` عند كل إقلاع، وTauri لا يوفّر مفتاحًا لتعطيله. التطبيق بلا نوافذ، فيُبقي macOS الطلب معلّقًا ويحققه عند **أول نافذة تظهر** — وهي لوح HUD. (خصائص اللوح نفسها — `NonactivatingPanel` و`orderFrontRegardless` — كانت سليمة.)
**الإصلاح (كله في طبقة النظام، بلا اعتماديات جديدة):**
- `sys/chrome.rs`: `resign_activation()` و`is_active()` (أضيفت خاصية `NSApplication` لـ`objc2-app-kit`). يُتنازل عن التنشيط فور الإقلاع في `lib.rs` — يكفي للإطلاق العادي.
- `hud::prime()`: يُوضع اللوح **شفافًا تمامًا** على الشاشة عند الإقلاع فيُستهلك الطلب المعلّق هناك لا أثناء الكتابة، مع مراقبة محدودة (30 فحصًا × 100ms، مرة واحدة) تُعيد التنشيط إن أخذه التطبيق وهو بلا نوافذ، ثم يُسحب اللوح. يلزم هذا للإطلاق الخلفي (عنصر تسجيل الدخول).
- خصائص اللوح صارت صريحة: `hidesOnDeactivate(false)` · `floatingPanel` · `becomesKeyOnlyIfNeeded` · `IgnoresCycle`.
- أداة تصحيح: `BADDEL_HUD_DELAY=<ثوانٍ>` تؤخّر أول إشعار ليمكن وضع تطبيق آخر في المقدمة قبله.

**طريقة الاختبار (بلا إدخال صناعي):** تشغيل بدّل ← فتح TextEdit ← انتظار أول HUD ← سؤال النظام عن التطبيق الأمامي (`lsappinfo front`) كل 1.5 ثانية.

| الحالة | قبل الإصلاح | بعد الإصلاح |
|---|---|---|
| إطلاق خلفي (`open -g`، كعنصر تسجيل الدخول) | ❌ Baddel في المقدمة | ✅ TextEdit يبقى |
| إطلاق عادي (`open`) | ❌ Baddel في المقدمة | ✅ TextEdit يبقى |
| إظهارات متكررة كل 400ms | ✅ | ✅ |
| HUD مرئي فعلًا والتطبيق غير نشط (لقطة شاشة كاملة) | — | ✅ |

`./scripts/check.sh` ✅ بعد الإصلاح (48 اختبارًا، clippy نظيف، `svelte-check` بلا أخطاء).

**يبقى من «ما تعذّر»:** لقطة قائمة شريط القوائم مفتوحةً (ليست من معيار القبول، وتحتاج نقرًا مركَّبًا). و`WebKit.Networking` التي تبقى لحظة بعد إغلاق النافذة تُقاس على بناء release في المرحلة 4 كما هو مسجّل.

### المرحلة 4 — الصقل والاختبار اليدوي — ✅ 2026-09-21

**البيئة:** macOS 27.0 · Xcode 27.0 · Rust 1.96.1 · Node 24.18.1 · npm 11.16.0.

**ما أُنجز**
- **المصفوفة** (`docs/test-matrix.md`): اختُبرت آليًا بإذن المستخدم TextEdit وNotes (جزئيًا) وSafari وBrave وChrome، في textarea وcontenteditable: **82 حالة ناجحة (52 منها بعد إصلاح المهلة)، دون أي إخفاق من التطبيق، والحافظة مستعادة في كل مرة**. ثم **أوقف المستخدم الاختبار على جهازه** («اكمل دون عمل اختبار على جهازي»)، فبقيت صفوف ⏳ وقائمة تجربة يدوية في آخر الملف. نُظّفت آثار الاختبار كلها: ملاحظة الاختبار في Notes (نُقلت إلى «المحذوفة مؤخرًا»)، ومستندات TextEdit غير المحفوظة، وألسنة صفحة الاختبار، واستُعيد نص الحافظة.
- **ضبط المهلات (الخطوة 3):** كانت Chromium وcontenteditable تستغرق نحو 250ms، لأنها تتجاهل كتابة AX فنلجأ إلى اللصق وننتظر `PASTE_SETTLE` ثابتًا. صار `Target::write` يراقب التحديد عبر AX حتى ينطوي، ثم يستعيد الحافظة فورًا. **النتيجة: 40–85ms** بدل 237–344ms. المهلة الثابتة باقية حدًّا أعلى، وللتطبيقات التي لا تكشف التحديد. كل المهلات ثوابت مسمّاة أعلى `controller.rs`.
- **الاستثناءات الافتراضية (الخطوة 4):** صارت 14 معرّفًا: 7 طرفيات (Terminal وiTerm وWarp وGhostty وkitty وAlacritty وWezTerm) و7 مديري كلمات مرور (Keychain Access وPasswords من Apple و1Password 7/8 وBitwarden وKeePassXC وEnpass). الاستثناء الافتراضي لتطبيق غير مثبّت يبقى نافذًا ولا يُعرض في القائمة، فلا تمتلئ اللوحة بمعرّفات عارية (`commands::excluded_apps`). **ملاحظة:** ملف إعدادات موجود مسبقًا يحتفظ بقائمته القديمة، والجديدة للتثبيتات الجديدة.
- **الخصوصية (الخطوة 5 والوكيل أ):** لا تسجيل لنص المستخدم (`trace!` وطباعة الأزمنة محذوفة من بناء release بـ`cfg(debug_assertions)`، ولا تطبع إلا أطوالًا ومسارات)، ولا كتابة له على القرص، ولا اتصال شبكي (الاستثناء الوحيد فتح رابطَي GitHub في متصفح المستخدم). CSP: `default-src 'self'` بلا أصل خارجي ولا `unsafe-eval`. **أصلحت ملاحظة الوكيل:** نص آخر تحويل كان يبقى في القائمة والذاكرة إلى أجل غير محدود، وحتى بعد تراجع فاشل. صار خيط المتحكّم يُسقطه عند انتهاء نافذة التراجع (30 ثانية) عبر `recv_timeout` دون مؤقّت جديد، والقائمة تعكس دائمًا ما يمكن التراجع عنه فعلًا (`Controller::sync_menu`).
- **الترجمة (الوكيل ب):** المفاتيح متطابقة ولا نصوص مكتوبة مباشرة في المكوّنات. أصلحت ما وجده: حُذف 13 مفتاحًا ميتًا (`common.add/cancel/ok` و`common.keys.*` و`shortcut.names.*` و`onboarding.openSystemSettings` و`onboarding.step3.successText`)، فصار في كل ملف 110 مفاتيح متطابقة. و`menu_text.rs` صار `hint` فيه مطابقًا لـJSON (الاختصار يُضاف في `tray::hint`). وأداة التصحيح `BADDEL_WINDOW=hud:*` صارت تقرأ نصوصها من `menu_text` بلغة الواجهة، وكانت تعرض العربية حتى في لقطات الإنجليزية. مفاتيح `menu.*` و`hud.*` لا تُقرأ في الواجهة، وهي باقية مرجعًا لجدول Rust.
- **القياس (الخطوة 6):** ذاكرة release في الخمول **15MB**، و33–35MB بعد فتح الإعدادات وإغلاقها. والهدف أقل من 40MB. التفاصيل في `test-matrix.md`.
- **بناء release كان معطّلًا على macOS 27:** `strip = true` يُطبَّق أيضًا على مكتبات الماكرو، فيرفض النظام تحميلها (`mis-aligned LINKEDIT string pool`). أضيف `[profile.release.build-override] strip = false`، والتطبيق الشحنة نفسه يبقى مجرَّدًا (5.2MB). **هذا كان سيوقف المرحلة 5.**

**مخرجات معيار القبول**
| البند | النتيجة |
|---|---|
| `./scripts/check.sh` | ✅ exit 0: 48 اختبارًا، وclippy نظيف، و`svelte-check` 0 أخطاء و0 تحذيرات، وبناء ناجح |
| `cargo clippy --workspace --all-targets -- -D warnings` | ✅ |
| مراجعة الوكيلين | ✅ أُغلقت ملاحظاتهما كلها (أعلاه) |
| `docs/test-matrix.md` مكتمل | ✅ كل صف فيه ✓ أو مستثنى بسبب موثَّق **أو منقول إلى طابور المرحلة 7** (بقرار المستخدم 2026-09-21: «قم بعمل ما يحتاج إلى اختبار لمرحلة لحدها»، فأُنشئت المرحلة 7) |

**الوكلاء (2 بالتوازي، sonnet، قراءة فقط):** أ (الخصوصية) وب (الترجمة). رُوجعت مخرجاتهما وتُحقّق من كل ملاحظة قبل إصلاحها.

**ما تعذّر / قرارات**
- **زمن مسار المفاتيح** (تطبيق بلا AX إطلاقًا) نحو 600ms في قياس المرحلة 2، والهدف 350ms. لم يُعَد قياسه في تطبيق Electron حقيقي. لا يُخفَّض دون قياس، لأن تقصير `COPY_TIMEOUT` أو `SELECTION_TIMEOUT` قد يكسر التطبيقات البطيئة.
- **ملاحظة المرحلة 2 عن شريط العنوان:** تقرّر ألا يُقيَّد التحويل، فالاختصار طلب صريح من المستخدم، و«تراجع» متاح 30 ثانية.
- `WebKit.Networking` (نحو 11MB) تبقى بعد إغلاق الإعدادات ما دام التطبيق يعمل. إنهاؤها يحتاج هدم مخزن بيانات WebKit، ولا تتيحه Tauri. المجموع ما زال قريبًا من الهدف.

**المرحلة 7 (جديدة):** بطلب المستخدم، صار الاختبار على الجهاز مرحلة مستقلة تُشغَّل عند الطلب ويمكن تكرارها. أضيفت إلى EXECUTION.md، ومعها قاعدة في CLAUDE.md، والأداة في المستودع `scripts/device-test/` (`run.sh` و`matrix.py` و`page.html`). الأداة تتحقق من التطبيق الأمامي قبل كل ضغطة، وتستعيد نص الحافظة بنفسها. الإطلاق (المرحلة 6) مشروط بأن يكون الطابور فارغًا.

**المطلوب منك:** حين تكون بعيدًا عن الجهاز اكتب «ابدأ المرحلة 7». وقبل المرحلة 5 يبقى قرار التوقيع.

### المرحلة 5 — الأيقونة والتوقيع والتحديث — ✅ 2026-09-21

**البيئة:** macOS 27.0 · Rust 1.96.1 (الهدفان arm64 وx86_64) · Node 24.18.1 · npm 11.16.0.
**قرار المستخدم:** لا اشتراك Developer ID ← **شهادة ذاتية ثابتة**. وطلب إكمال كل ما بقي معلّقًا من المراحل السابقة.

**ما أُنجز**
- **الأيقونة (الخطوة 1):** `icon.icns` داخل حزمة release مطابق بايتًا ببايت لـ`src-tauri/icons/icon.icns` المصدَّر من Figma، وDMG يحمل `.VolumeIcon.icns`.
- **التوقيع (الخطوة 2):** `scripts/signing/create-keys.sh` أنشأ في `~/.baddel/` (خارج المستودع، والصلاحيات 600) هوية «Baddel Code Signing» (RSA 3072، صالحة 20 سنة) ومفتاح المحدِّث، وكلمتا المرور مولَّدتان عشوائيًا. بصمة الشهادة: `ABEADCF9647763862BC4924C07DA7E5A7FD777B0`. `scripts/signing/signing-env.sh` يوقّع عبر سلسلة مفاتيح مؤقتة، **ولا يمس سلسلة الدخول**. `dev-build.sh` يوقّع نسخة debug بالهوية نفسها. التوثيق في `docs/signing.md`.
  - `APPLE_CERTIFICATE` في Tauri يرفض أي هوية لا يبدأ اسمها بـ«Apple Development:» ونحوه، **وحين فشل ترك سلسلة مفاتيح فيها نسخة من هويتنا في قائمة بحث المستخدم**. أُزيلت فورًا (تُحقّق منها قبل الحذف: أُنشئت في دقيقة الفشل نفسها وفيها «Baddel Code Signing»)، واستُبدل بها تمرير البصمة في `APPLE_SIGNING_IDENTITY`.
- **المحدِّث (الخطوة 3):** `tauri-plugin-updater` مسجَّل، والمفتاح العام وendpoint (`releases/latest/download/latest.json`) في `tauri.conf.json`، و`createUpdaterArtifacts`. `src-tauri/src/updater.rs`:
  - **تلقائي:** بعد 20 ثانية من الإقلاع، ثم كل 24 ساعة (يُحفظ `lastUpdateCheck`). لا يعرض حوارًا، بل يضيف عنصر قائمة.
  - **من القائمة:** حوار أصيل للنتيجة («ثبّت وأعد التشغيل» / «لاحقًا»، أو «محدَّث»، أو «تعذّر»). ينشّط التطبيق للحوار ثم يعيد التنشيط لما كان قبله.
  - **من الإعدادات:** الحالة في وصف صف «التحديثات»: «الإصدار X · آخر تحقق …» (**أُنجز ما عُلّق من المرحلة 3**)، و«الإصدار Y متاح» مع زر «ثبّت التحديث». رُوجع بصريًا في المتصفح عبر `mock.ts` (`?update=available`).
  - نصوص الحوارات في `menu_text.rs` ونسختها المرجعية `updateDialog.*` في JSON. في كل ملف 126 مفتاحًا متطابقة.
- **الإصدار (الخطوة 4):** `scripts/release.sh <version> [notes]` يرفع النسخة في `package.json` (مع lock) و`Cargo.toml` و`tauri.conf.json`، ويبني `universal-apple-darwin` موقَّعًا، ويتحقق من التوقيع والـDR، ويجمع الناتج في `release/<v>/` (مستثنى من git). `bundle.targets` صار `["app", "dmg"]`.
- **معلّقات سابقة أُغلقت:** (1) «آخر تحقق» (المرحلة 3). (2) ترحيل الاستثناءات: `defaultsVersion` في الإعدادات. الملف القديم يُقرأ بالإصدار 1، ويأخذ مرة واحدة ما أضيف بعده، ولا يُعاد ما حذفه المستخدم (اختباران في `settings.rs`). (3) DMG وتسجيل المحدِّث (المرحلة 0).

**مخرجات معيار القبول**
| البند | النتيجة |
|---|---|
| `./scripts/release.sh 0.9.1` | ✅ exit 0 ← `Baddel_0.9.1_universal.dmg` (7.3MB) و`.app.tar.gz` (6.8MB) و`.sig` و`latest.json` |
| الحزمة | ✅ `lipo`: x86_64 arm64 · `codesign --verify --deep --strict` ناجح · `flags=0x10000(runtime)` · `Authority=Baddel Code Signing` |
| DR ثابت | ✅ `identifier "com.isltanx.baddel" and certificate leaf = H"abeadcf9…77b0"`، وهو نفسه للتطبيق داخل DMG (رُكّب للقراءة فقط دون أن يظهر في Finder) |
| توقيع المحدِّث | ✅ معرّف المفتاح في `.sig` يطابق المفتاح العام المضمَّن |
| `spctl -a -vv` | `rejected`، وهذا متوقع بلا Developer ID (البند مشروط به) |
| لا أسرار في المستودع | ✅ `git grep -nE "BEGIN .*PRIVATE\|encrypted secret key" -- ':!EXECUTION.md'` فارغ، ولا قيمة من `secrets.env` في المستودع. الأمر الأصلي (`password`) عُدّل في EXECUTION.md لأنه يصيب قائمة الكلمات وأسماء المتغيرات |
| التثبيت على حساب نظيف + اختبار التحديث | ⏳ في طابور المرحلة 7 (البنود 12–14). اختبار التحديث يحتاج `latest.json` متاحًا للعموم |
| `./scripts/check.sh` | ✅ exit 0: 50 اختبارًا · clippy نظيف · `svelte-check` 0/0 · بناء ناجح |

**ملاحظات**
- النسخة في المستودع صارت **0.9.1** (من تشغيل `release.sh` لمعيار القبول).
- ⚠️ **تغيّرت هوية توقيع نسخة التطوير** (من شهادة Apple Development التي تحمل بريدك في اسمها إلى «Baddel Code Signing»). بعد أول `dev-build.sh` سيطلب macOS منح صلاحية Accessibility مرة أخيرة، ثم تثبت لكل البناءات والإصدارات.
- Notarization متجاوَز (تحذير Tauri متوقع).

**المطلوب منك**
1. **انسخ `~/.baddel` احتياطيًا** في مكان آمن، فهو النسخة الوحيدة. ضياعه يُفقد المستخدمين الصلاحية أو التحديث التلقائي.
2. بعد أول بناء جديد: امنح الصلاحية مرة أخيرة (إعدادات النظام ← الخصوصية والأمان ← تسهيلات الاستخدام: احذف «بدّل» القديم ثم أضفه).
3. «ابدأ المرحلة 7» متى كنت بعيدًا عن الجهاز، أو «ابدأ المرحلة 6» للإطلاق (شرطها أن يكون الطابور فارغًا).

### المرحلة 6 — الإطلاق — 🟡 2026-09-21 (كل ما لا يمسّ الجهاز ولا يُنشر)

**البيئة:** macOS 27.0 · Xcode 27.0 · Rust 1.96.1 · Node 24.18.1 · npm 11.16.0 · gh 2.96.0.
**قرار المستخدم:** شرط المرحلة (طابور فارغ) غير متحقق، فالمطلوب الآن «ما لا يمسّ الجهاز»، والنشر يبقى حتى يفرغ الطابور وتوافق صراحةً.

**ما أُنجز**
- **README.md** (عربي) على مستوى رفّ وجُسور: غلاف بالوضعين، وشارات، وتنزيل، و«ما هو بدّل؟»، و«كيف يعمل» بأمثلة **متحقَّق منها بالنواة نفسها** في تخطيطَي Mac وPC، ومفتاح «لا»، و«لماذا بدّل؟»، وجولة لقطات حقيقية بالوضعين، والاختصارات، والتثبيت مع تنبيه Gatekeeper (macOS 15+ و13–14 وxattr)، والصلاحية، والخصوصية، والتحديث، و«أين يعمل» بصدق (المختبَر، وغير المختبَر، والمستثنى)، وعشرة أسئلة متكررة، وقسم المطوّرين، والحقوق («تصميم وتطوير: سلطان» + المستودع + «من الصانع نفسه»). كل ادعاء فيه قورن بالكود أو بسجل المراحل، **وصُحّح ثلاثة قبل الاعتماد**: Notes مختبَر في «آخر كلمة» وحدها، والأرقام تتبع اتجاه التحويل («فثسف ١٢٣» ← `test 123`)، ولا ادعاء «التحديثات بلا تحذير Gatekeeper» لأنه لم يُختبر.
- **README.en.md** (الوكيل أ) · **CHANGELOG.md** و**PRIVACY.md** (الوكيل ب، PRIVACY بالعربية ثم الإنجليزية).
- **الغلاف وصورة المعاينة:** `design/brand/cover.html` بهوية غلاف Figma (مسرح الأيقونة، والعنوان، وبطاقة التحويل بالمفاتيح، والوعد، والحقوق) ورموز `theme.css` نفسها. `design/brand/render.sh` يصدّرها عبر Chrome بلا واجهة (لا نافذة ولا تركيز، 5 ثوانٍ): `docs/assets/cover-light.png` و`cover-dark.png` (2560×1120) و`social-preview.png` (1280×640).
- **GIF:** السيناريو والتجهيز والمنطقة والتوقيت وأمر `ffmpeg` في `docs/launch/demo-gif.md`. مكانه في README معلَّم بتعليق، وتقوم مقامه لقطة الإشعار إلى أن يُسجَّل.
- **منشور الإطلاق:** `docs/launch/announcement.md` (قصير 229 حرفًا + متوسط).
- **النشر مُجهَّز لا مُنفَّذ:** `scripts/publish.sh <v>` يفحص الملفات الأربعة، وأن `latest.json` يشير إلى الوسم ويحمل توقيع الأرشيف، ونظافة `main`، وملاحظات الإصدار من CHANGELOG (ويرفض «لم يُنشر بعد»)، ثم **لا ينشر إلا بـ`--yes`**. جُرِّب جافًا على 0.9.1: فحصا الملفات و`latest.json` ✓، وتوقّف عند شجرة العمل غير النظيفة كما يجب. الخطوات كلها بترتيبها في `docs/launch/release-checklist.md` (وأشارت إليها الخطوة 5 في EXECUTION.md).
- **`scripts/check-docs.py`:** يفحص الروابط والصور وروابط الأقسام (بقاعدة GitHub) في README.md وREADME.en.md وCHANGELOG.md وPRIVACY.md، والروابط الخارجية مع `--online`.
- **اللقطات الـ42 أُعيد التقاطها** بإذن المستخدم: كانت لوحة «عام» تعرض 0.1.0 وزر «تحقق الآن» معطّلًا، و**أربع من لقطات HUD الإنجليزية الخمس مطابقة للعربية بايتًا ببايت** (التُقطت قبل إصلاح المرحلة 4). الآن: 0.9.1، والزر مفعّل، وHUD الإنجليزي بنصه وسهم `→`.
- **`capture.sh` صار آمنًا لمن يستخدم بدّل:** ينسخ ملف الإعدادات ويعيده عند الخروج، ويعيد تشغيل نسخة بدّل التي كانت تعمل (`open -g`، في الخلفية). تحقّق: الملف عاد مطابقًا، ثم رحّلته النسخة الجديدة عند إقلاعها (`defaultsVersion` 1 ← 2: أضيفت الاستثناءات الافتراضية التسعة الجديدة وبقيت القديمة) — **أول تحقق فعلي من ترحيل المرحلة 5 على ملف حقيقي**.

**تدقيق ما قبل جعل المستودع عامًا**
- لا مفتاح خاص ولا قيمة من `~/.baddel/secrets.env` في **كل تاريخ git** (فُحصت القيم نفسها، لا الأنماط فقط). المطابقة الوحيدة اسم الهوية «Baddel Code Signing»، وليس سرًّا.
- لا بريد ولا مسار شخصي في الملفات المتتبَّعة.
- **الخطة §5 «تحقّق أن رفّ نفسه يحترم هذين الوسمين»:** بقراءة كود رفّ (`src-tauri/src/macos.rs`) يتجاهل `ConcealedType` و`AutoGeneratedType`، لا `TransientType`. وبدّل يضع `ConcealedType` دائمًا، فرفّ يتجاهل ما يمرّ منه. (بقراءة الكود، لا بتجربة على الجهاز.)

**مخرجات معيار القبول (ما يمكن فحصه قبل النشر)**
| البند | النتيجة |
|---|---|
| روابط الوثائق الأربع والصور وروابط الأقسام | ✅ `python3 scripts/check-docs.py` ← 100 رابط، 0 مشكلات |
| الصور بالوضعين | ✅ الغلاف واللقطات في `<picture>` بمصدر داكن. العرض على GitHub نفسه يُفحص بعد النشر |
| `./scripts/check.sh` | ✅ exit 0: 50 اختبارًا، وclippy نظيف، و`svelte-check` 0/0، وبناء الواجهة ناجح |
| تنزيل DMG من صفحة الإصدار على جهاز آخر ← تثبيت ← ترحيب ← تحويل | ⏳ بعد النشر (الخطوة 12 في قائمة الإصدار) |
| STATUS.md: كل المراحل ✅ | ⏳ المرحلة 7 ثم النشر |

**ما تعذّر / ينتظر**
- **النشر** (جعل المستودع عامًا، والوسم، والإصدار، وصورة المعاينة): ينتظر فراغ الطابور وموافقتك الصريحة.
- **GIF العرض:** يسجّله المستخدم.
- **لقطتا «عام» و«حول»** تعرضان 0.9.1، وتُعادان برقم 1.0.0 عند الإصدار (الخطوة 3 في القائمة).

**الوكلاء (2 بالتوازي، sonnet)**
- أ (README.en.md): رُوجع كاملًا. التسميات من `en.json` حرفيًا، والأقسام كلها تُحلّ. صُحّح: «Keychain» ← «Keychain Access»، و«confidential» ← «concealed»، وسهم HUD الإنجليزي `→`، وأسماء صلاحيات النظام بحروفها الكبيرة.
- ب (CHANGELOG وPRIVACY): رُوجعا مقابل الكود. صُحّح: ترتيب اللوحات (عام ثم الاختصارات ثم التخطيطات)، وأسماء حالات القائمة كما في الواجهة، وحالة «التحديد طويل جدًا» الناقصة، و«لا يُقرأ شيء بعد ذلك» (التراجع يقرأ النص المحوَّل أيضًا). ونبّه الوكيل بحق إلى أن «1Password» في README اسم لمعرّفين (7 و8)، فالعدد 7 مديري كلمات مرور صحيح.

**تنسيق README لعرض GitHub — 2026-09-21** (بطلبك بعد الـcommit `d4db17c`)
عُرض README بمحرّك GitHub نفسه (`gh api markdown`، بوضع `gfm` لأنه وحده يضيف `dir="auto"` والتنبيهات كصفحة المستودع، مع دمج الأسطر المتتالية كما تُعرض الملفات)، ثم التُقط بـChrome بلا واجهة بالوضعين. ظهر ما لا يُرى في المحرر:
- GitHub يضع `dir="auto"` على الفقرات والقوائم والعناوين، **لا على الجداول ولا `<details>`**: فالجداول الستة والأسئلة العشرة كانت ستُعرض من اليسار، وسطر مثل «لماذا يحذّرني macOS…؟» بأجزاء مقلوبة. ← المحتوى كله داخل `<div dir="rtl">` (تحقّق أن GitHub يُبقي `dir="rtl"` على `div` و`table` و`details`، ويحذف `bdo` و`bdi`).
- القائمة أو الفقرة التي تبدأ بكلمة لاتينية («macOS 13…»، «Apple Silicon…») يجعلها `dir="auto"` من اليسار. ← أُعيدت صياغتها لتبدأ بالعربية.
- الأكواد التي تبدأ أو تنتهي بعلامة (`why?` و`;dt hgphg?` و`'self'` و`~/Library/…`) تنقلب أطرافها. ← `<span dir="ltr">`. وتسلسل ⌥⇧Space بقي من اليسار إلى اليمين كما في التطبيق والغلاف.
- العربية داخل كتل الأوامر تنفصل حروفها بالخط أحادي العرض. ← الأوامر وبنية المشروع صارتا جدولين.
- التنبيه `[!IMPORTANT]` لا يُعرض داخل `<div>`. ← يُغلق الغلاف قبله ويُفتح بعده.
- shields.io يمطّ نص الشارة فيكسر وصل الحروف العربية. ← تسميات الشارات بالإنجليزية.
- «macOS 13+» و«Node 20+» كانت تُعرض «+macOS 13»، و«مرخَّصة بـMIT — © … Sultan Al-Anzi» كانت تُقرأ «مرخَّصة بـSultan». ← «أو أحدث»، والحقوق في ملف الرخصة والتذييل.
- الجولة: الصور بمحاذاة علوية، وأزواج متقاربة الارتفاع (الترحيب·الصلاحية، عام·حول، الاختصارات·الاستثناءات)، و«التخطيطات» وحدها بعرض 72٪. وصور حالات HUD بارتفاع واحد بدل عرض واحد (كانت «تم التراجع» ضعف أخواتها). والنسخة الإنجليزية بالترتيب نفسه.
- **القواعد صارت فحصًا دائمًا** في `scripts/check-docs.py` (`rtl_problems` على README.md): يلتقط في النسخة السابقة 30 مشكلة، والحالية 0. وطُبّق على CHANGELOG وPRIVACY: لُفّ مسار الإعدادات في PRIVACY، وعنوان `## v1.0.0 — …` في CHANGELOG يبقى لاتينيًا (نمط رفّ، و`publish.sh` يعتمد عليه).

**المطلوب منك**
1. راجع README.md (والغلاف في `docs/assets/`). **قرارك (2026-09-21): الدفع إلى GitHub بعد المرحلة 7**، لا قبلها.
2. قرّر ما يبقى عند جعل المستودع عامًا: `STATUS.md` و`EXECUTION.md` و`CLAUDE.md` (سجل العمل)، و`docs/design/stitch/` (تصميم Stitch المرفوض)، و`design/icon/concepts/` (مفاهيم الأيقونة المرفوضة)، و`docs/baddel-figma-prompt*.md`. كلها تصير علنية كما هي الآن.
3. «ابدأ المرحلة 7» حين تبتعد عن الجهاز، ثم «كمّل المرحلة 6» للنشر.
4. سجّل GIF العرض حسب `docs/launch/demo-gif.md` متى شئت.

### المرحلة 7 — الاختبار على الجهاز — ✅ 2026-09-22

**البيئة:** macOS 27.0 · Xcode 27.0 · Rust 1.96.1 · Node 24.18.1 · npm 11.16.0.
**تعليمة المستخدم:** «ابدأ المرحلة 7 ولا ترجع لي، انت القائد المتحكم». لذلك **أُصلح ما كشفه الاختبار في الجلسة نفسها** وأُعيد اختباره، بدل تأجيله إلى جلسة لمرحلته الأصلية كما تقول الخطوة 6.

**النتائج** (التفاصيل والأزمنة في `docs/test-matrix.md`، وكلها على البناء النهائي)
| # | البند | النتيجة |
|---|---|---|
| 1 | الجزء الآلي: TextEdit، وSafari وBrave وChrome (textarea وcontenteditable وحقل كلمة مرور) | ✅ 31/31، و18–86ms |
| 2 | Notes: الحالات الأربع | ✅ 8/8، و7–38ms |
| 3 | Mail: الحالات الأربع في رسالة جديدة، لم تُرسل وأُغلقت «عدم الحفظ» | ✅ 8/8، ⚠️ بطيء: 1.2 ثانية (مسار المفاتيح) |
| 4 | Claude.app (Electron) | ✅ 8/8 بعد إصلاح، 42–94ms، و343–375ms حين يلزم اللصق |
| 5 | Figma: نص على اللوحة في ملف مسودة جديد | ✅ 8/8 بعد إصلاح، 265–607ms. أول ضغطة بعد دخول وضع التحرير قد تعطي «لا نص» |
| 6 | Luma | ✅ 8/8، 47–63ms |
| 7 | Terminal | ✅ `Excluded` |
| 8 | Spotlight | — معطّل على هذا الجهاز (اختصاره مغلق، و`Spotlight.app` يرفض التشغيل). «HUD لا يسحب التركيز» تحقّق ضمنيًا في مئات التحويلات: الحراسة تفحص المقدمة قبل كل ضغطة |
| 9 | Chrome بعد تشغيله مباشرة | ✅ أول ضغطة 1.23 ثانية ثم 64–76ms. لا أثر آخر ظاهر |
| 10 | آخر تحويل يختفي من القائمة بعد 30 ثانية | ✅ حاضر عند ~27 ثانية، وغائب عند ~32 |
| 11 | لقطة القائمة مفتوحةً | ✅ `docs/screenshots/menu-ar-dark.png` و`menu-en-dark.png`. الفاتح ← البند 16 |
| 13 | «تحقّق من التحديثات…» والمستودع خاص | ✅ «تعذّر التحقق من التحديثات» (بعد إصلاح زره). البقية ← البند 13 |

**ما كشفه الاختبار وأُصلح في الكود** (كل إصلاح أُعيد اختباره على الجهاز)
1. **حقل كلمة مرور ويب في Safari لم يُحجب**، فـSafari لا يفعّل الإدخال الآمن فيه. صار العنصر بدور `AXSecureTextField` محجوبًا بذاته (`text_access.rs`: `is_secure`، و`controller.rs`).
2. **Claude.app: «تم التحويل» والنص لم يتغيّر.** ProseMirror يقبل كتابة AX ويتجاهلها. صار يُتحقق من النص بعد الكتابة، ويُلصق إن بقي الأصل بعينه (`write_dropped`، و`WRITE_SETTLE` = 100ms).
3. **contenteditable في Safari** لا يكشف `AXSelectedText`، فكان يسلك مسار المفاتيح (600ms). صار النص يُشتق من النطاق (43–64ms).
4. **Figma: «لا نص» دائمًا.** التركيز على عنصر بديل فارغ. صار العنصر الذي ليس حقل نص ويبلّغ عن صفر حروف يُحال إلى مسار المفاتيح (`is_stand_in`). **الحقل الفارغ حقًا مستثنى**: النسخة الأولى من الإصلاح أحالته هو أيضًا، فقرأ مسار المفاتيح في مستند فارغ حافظةً كتبتها عملية أخرى أثناء انتظاره ولصق تحويلها. كشفه اختبار المستند الفارغ في الإعادة الأخيرة، وضُيّق الشرط قبل الاعتماد.
5. **طلب صلاحية من النظام معلّق على الشاشة يعطّل بدّل في كل التطبيقات** («لا نص»): الاستعلام عن العنصر المركَّز على مستوى النظام يفشل. صار يُسأل التطبيق الأمامي عند فشله (`Focused::get`).
6. **بعد إطلاق بدّل صار هو التطبيق النشط** (حتى بـ`open -g`)، فذهبت الضغطات إلى لا شيء. `deactivate()` وحده لم يكفِ في هذه الحال، فصار `resign_activation` يُنشّط التطبيق المالك لشريط القوائم مباشرة. تحقّق في الإطلاق الخلفي والعادي.
7. **القائمة:** الاختصار كان يظهر «Space⇧⌥» في العربية، وسطر آخر تحويل بسهم معكوس في الإنجليزية («hello → اثممخ»). عُزل الاختصار، وثُبّت اتجاه سطر آخر تحويل على لغة الواجهة.
8. **حوار المحدِّث** بزر «OK» إنجليزي في العربية. صار الزر من `menu_text` («حسنًا»)، ومفتاح `updateDialog.ok` في JSON (127 = 127).

**أداة الاختبار (أُصلحت، فكانت تختبر غير ما تظن)**
- `open -b … file://…#field` يُسقط الجزء بعد `#`، **فحالات contenteditable وكلمة المرور في أول تشغيل لهذه الجلسة كانت كلها textarea** (أداة المرحلة 4 كانت نسخة سابقة، ومساراتها المسجّلة تدل على contenteditable فعلًا). صار `run.sh` يفتح الألسنة بقاموس كل متصفح، و`page.html` يضع المؤشر ويتبع `hashchange` ولا يستعمل `autofocus`.
- الحراسة صارت من Launch Services (System Events ظل يسمّي TextEdit وبدّل هو من يستقبل الضغطات) ومعها مالك تركيز لوحة المفاتيح (`focus-owner.swift`). و`BADDEL_TRUST_FRONT=1` مخرج صريح حين يعجز النظام عن تسميته، استُخدم بعد تحقق يدوي.
- جديد: `focus-field.swift` (تركيز حقل عبر AX بلا فأرة) و`cleanup.sh`. وحُدّثت الخطوات 3–5 في EXECUTION.md بما تعلّمناه.

**مخرجات معيار القبول**
| البند | النتيجة |
|---|---|
| لكل بند نُفّذ سطر نتيجة في `docs/test-matrix.md` | ✅ (وقسم «المرحلة 7» فيه) |
| الطابور محدَّث | ✅ 5 بنود باقية بأسبابها |
| `./scripts/check.sh` | ✅ exit 0: 50 اختبارًا، وclippy نظيف، و`svelte-check` 0/0، وبناء ناجح |
| `cargo clippy --workspace --all-targets -- -D warnings` | ✅ |
| `python3 scripts/check-docs.py` | ✅ 104 روابط، 0 مشكلات |

**التنظيف** (منفَّذ ومتحقَّق منه)
- ألسنة `page.html` أُغلقت في المتصفحات الثلاثة، وألسنتك في Brave كما هي.
- 7 مستندات TextEdit غير محفوظة: أُهملت عبر «حذف» (لم يُحفظ شيء؛ رُفض «حفظ وإنهاء»).
- ملاحظة الاختبار في Notes (أُنشئت مرتين): إلى «المحذوفة مؤخرًا».
- مسودتا Mail: أُغلقتا بـ«عدم الحفظ»، ولم تُرسل رسالة.
- رسالة Claude.app وحقل Luma: فُرّغا، والحقلان كانا فارغين قبل الاختبار.
- أُغلقت التطبيقات التي شغّلتها الجلسة: Safari وChrome وTextEdit وMail وLuma وTerminal. والعملية الظاهرة الآن هي نفسها قبل الجلسة: Finder وFigma وChatGPT وNotes وBrave وManus وClaude.
- مصدر الإدخال ABC كما كان (بعد أن بدّله ⌘Space مرة، وأعدته). والحافظة بنصها الأصلي. وإعدادات بدّل مطابقة للنسخة قبل لقطات الإنجليزية (`cmp`). ونسخة بدّل (debug، البناء النهائي) تعمل في الخلفية كما كانت.

**لم يُنظَّف (خارج يدي)**
- ملف Figma «Baddel device test (scratch — safe to delete)» في مسوداتك: https://www.figma.com/design/sM22U82d7uEyq2106eBBNH. لا يتيح Figma MCP الحذف.
- سجل Raff قد يحوي نصوص الاختبار («مرحبا اثممخ» وأخواتها ونص الحافظة المؤقت).

**المطلوب منك**
1. **طلبان من النظام معلّقان على الشاشة، ولم أُجب عنهما**، فهما قرار صلاحيات: (أ) «يريد Claude الوصول للتحكم في البريد» (من AppleScript أرسلته إلى Mail، وتجاوزته بعده)؛ (ب) «يطلب Claude تجاوز منتقي النوافذ الخاصة…» (من `screencapture`). وجودهما هو ما كشف الخللين 5 و6.
2. احذف ملف Figma المؤقت، وامسح سجل Raff إن شئت.
3. بعد الطلبين: البند 15 (دقيقتان). ثم «كمّل المرحلة 6» للنشر، وبعده البنود 13 و14، والبند 12 على حساب نظيف.

