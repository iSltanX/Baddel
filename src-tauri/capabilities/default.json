{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the settings and onboarding windows.",
  "windows": ["settings", "onboarding"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
