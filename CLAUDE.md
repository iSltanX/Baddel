# بدّل — Baddel

أداة شريط قوائم لنظام macOS تصحّح النص المكتوب بتخطيط لوحة المفاتيح الخطأ (عربي ⇄ إنجليزي) باختصار واحد.
Tauri 2 (نواة Rust + واجهة Svelte/TypeScript)، محلي بالكامل، يحتاج صلاحية Accessibility فقط.

## بروتوكول المراحل — اقرأه قبل أي عمل

المشروع يُنفَّذ على مراحل معرّفة في [EXECUTION.md](EXECUTION.md). حين يقول المستخدم **«ابدأ المرحلة X»**:

1. اقرأ [STATUS.md](STATUS.md) لتعرف ما اكتمل. لا تعتمد على ذاكرة أي محادثة سابقة. كل الحالة في هذا المستودع.
2. اقرأ قسم المرحلة X في [EXECUTION.md](EXECUTION.md) كاملًا، والأقسام التي يحيل إليها في [docs/baddel-implementation-plan.md](docs/baddel-implementation-plan.md).
3. تحقق أن المرحلة السابقة مكتملة في STATUS.md. إن لم تكن مكتملة، أخبر المستخدم واسأله قبل المتابعة.
4. نفّذ «فحص البيئة» أول المرحلة. هذه الجلسة قد تكون على جهاز مختلف عن آخر مرة.
5. نفّذ خطوات المرحلة **ضمن نطاقها فقط**. ما يخص مرحلة لاحقة يُسجَّل في «ملاحظات» بـSTATUS.md ولا يُنفَّذ.
6. شغّل «معيار القبول» فعليًا وانقل مخرجاته. لا تعلن اكتمال مرحلة لم تمرّ أوامر قبولها.
7. حدّث STATUS.md: الحالة، التاريخ، ما أُنجز، ما تعذّر، وما يُطلب من المستخدم.
8. اختم بتقرير قصير، واقترح commit برسالة جاهزة. **لا تنفّذ commit أو push إلا بموافقة المستخدم.**

تنويعات مقبولة من المستخدم: «كمّل المرحلة X» (استئناف من آخر خطوة في STATUS.md)، و«وين وصلنا؟» (لخّص STATUS.md فقط).

## سياسة الوكلاء (Subagents)

- الجلسة الرئيسية تنفّذ العمل الجوهري بنفسها: المعمارية، الخوارزميات، طبقة النظام، والواجهة.
- الوكلاء **فقط** للمهام البسيطة المعزولة التي يسمّيها قسم «الوكلاء» في كل مرحلة. لا تفوّض غيرها.
- **الحد الأقصى: وكيلان، يعملان بالتوازي** (يُطلقان في رسالة واحدة)، وكلاهما بـ `model: "sonnet"`.
- برومبت الوكيل مكتفٍ بذاته: مسارات الملفات، الصيغة المطلوبة للمخرجات، وحدود ما يلمسه.
- راجع مخرجات الوكيل قبل اعتمادها. الوكيل لا يعدّل ملفات خارج ما حُدّد له.

## ثوابت المشروع

- الحد الأدنى: macOS 13. البناء: Xcode Command Line Tools + Rust stable + Node 20+، ومدير الحزم npm (على نمط Luma).
- Tauri 2 + Svelte 5 + TypeScript + Vite. Cargo workspace: `crates/baddel-core` و`src-tauri`.
- المنطق الصِرف في `crates/baddel-core`، **بلا اعتماد على Tauri ولا على macOS**، ويُختبر بـ `cargo test -p baddel-core`.
- كل `unsafe` واستدعاءات النظام (AX وCGEvent وNSPasteboard وCarbon TIS) محصورة في `src-tauri/src/sys/` خلف واجهات آمنة.
- الاعتماديات المسموحة — إضافات Tauri: `global-shortcut` و`autostart` و`updater` و`store` و`process` و`dialog`. حزم Rust للنظام: `core-foundation` و`core-graphics` و`objc2` و`objc2-foundation` و`objc2-app-kit` و`accessibility-sys`. أي إضافة أخرى تحتاج موافقة المستخدم.
- القائمة وHUD **أصيلان** (tray menu وNSPanel) لا webview. نافذتا الإعدادات والترحيب تُنشآن عند الطلب وتُدمَّران عند الإغلاق، فلا عملية webview في الخمول.
- كل حالة التطبيق ومنطقه في Rust، والواجهة عرض فقط عبر `invoke` والأحداث.
- لا شبكة إلا للمحدِّث. الخطوط والأصول كلها محلية وCSP مقيَّدة. لا تسجيل لأي نص محوَّل. لا Input Monitoring.
- الواجهة عربية RTL أولًا مع إنجليزية كاملة، والنصوص في `src/lib/i18n/{ar,en}.json` (وجدول صغير في Rust للقائمة وHUD). المواصفة المعتمدة للواجهة: [docs/baddel-figma-prompt-pro.md](docs/baddel-figma-prompt-pro.md).
- الخطوط: Cairo للعناوين (600/700)، وAlmarai لبقية النصوص (400/700)، مضمَّنة في الحزمة (رخصة OFL).
- استخدم مسارات نسبية لجذر المستودع دائمًا. لا تفترض `/Volumes/iSltanXx`.
